# 2026-04-30 — Preview-деплой (Vercel fra1) — end-to-end smoke

Реализован milestone «Preview-деплой (Vercel fra1)» из `.ai-factory/ROADMAP.md`. End-to-end smoke: `/start` + текстовое сообщение к `@dev_test_podpiska_bot` на preview-деплое → запись в Supabase (`users` / `conversations` / `messages`).

## Решения (краткое зеркало плана)

- Repository-функции принимают `db` явно как параметр + опциональный pino-совместимый `RepoLogger` (см. `packages/db/src/repositories/logger.ts`). Это держит границу `@oplati/db` без зависимости от pino и облегчит мокирование в Sprint 2 vitest'ах.
- `users` upsert — через raw SQL (`db.execute(sql\`...\`)`), потому что partial unique index `WHERE telegram_id IS NOT NULL` в Drizzle 0.45 на `onConflictDoUpdate` требует ручного `targetWhere`-протокола; raw SQL детерминированнее и совместим с `prepare=false` (Supabase pooler).
- `(xmax = 0) AS created` в `RETURNING` — Postgres-трюк отличить INSERT от UPDATE.
- `display_name = COALESCE(EXCLUDED.display_name, users.display_name)` — Telegram-апдейт без `first_name` (anonymous, deep-link bot start) не перезатирает уже сохранённое имя.
- Запись в БД — синхронная до возврата 200 OK; ошибки БД глотаются (Sentry + log), AI-ответ всё равно уходит. Это явное graceful degradation — webhook не молчит при падении Postgres.
- AI-history НЕ загружается из БД на этом milestone — изолируем риск; подключим в milestone «State machine + AI tools».
- `preferredRegion='fra1'` + `maxDuration` (`/api/bot` 30s, `/api/health` 5s) — выровняли API routes с `docs/deployment.md`.

## Smoke runbook

> Заполняется по ходу прогона. Команды из `docs/deployment.md` раздел «Telegram webhook — preview».

### Шаг 1 — Push + ожидание Vercel preview

```bash
git push -u origin feature/preview-deploy-vercel
# Получить preview-URL из:
#   - комментария Vercel-бота в PR (создать после первого зелёного билда)
#   - vercel ls --scope <team> | head
#   - branch-alias: oplati-podpisku-web-git-feature-preview-deploy-vercel-<team>.vercel.app
```

**Ожидание:** Vercel build статус `Ready` за ~2-3 мин.

**Результат:**

- **Preview URL:** `<заполнить>`
- **Build duration:** `<заполнить>`
- **Build status:** `<заполнить>`

### Шаг 2 — Перерегистрация webhook dev-бота

```bash
PREVIEW_URL="<preview-url-из-шага-1>"
TG_TOKEN="<TELEGRAM_BOT_TOKEN_DEV из Vercel Preview env>"
TG_SECRET="<TELEGRAM_WEBHOOK_SECRET_DEV из Vercel Preview env>"

curl -F "url=${PREVIEW_URL}/api/bot" \
     -F "secret_token=${TG_SECRET}" \
     -F "drop_pending_updates=true" \
     -F 'allowed_updates=["message"]' \
     "https://api.telegram.org/bot${TG_TOKEN}/setWebhook"

curl "https://api.telegram.org/bot${TG_TOKEN}/getWebhookInfo"
# Ожидание: url=$PREVIEW_URL/api/bot, pending_update_count=0, last_error_message=""
```

**Результат:** `<заполнить getWebhookInfo output>`

### Шаг 3 — Telegram /start

В `@dev_test_podpiska_bot` отправить `/start`. Ожидание: текст из `GREETING` (`packages/agent/src/prompts.ts`).

- **`update_id` (из Vercel Logs):** `<заполнить>`
- **Ответ бота:** `<заполнить — copy/paste>`

### Шаг 4 — Telegram текстовое сообщение

Отправить «Привет, нужна подписка Claude». Ожидание: AI-ответ Claude.

- **`update_id`:** `<заполнить>`
- **`durationMs` AI-call:** `<из логов `telegram.message.ai_reply`>`
- **`input_tokens` / `output_tokens`:** `<из логов>`

### Шаг 5 — SQL через Supabase MCP

```sql
-- 5.1 пользователь
SELECT id, telegram_id, display_name, language, created_at
FROM users
ORDER BY created_at DESC
LIMIT 5;
```

```sql
-- 5.2 conversation
SELECT id, user_id, channel, handoff_mode, assigned_operator_id, created_at
FROM conversations
WHERE user_id = '<id из 5.1>';
```

```sql
-- 5.3 messages
SELECT role, content, meta, created_at
FROM messages
WHERE conversation_id = '<id из 5.2>'
ORDER BY created_at;
```

**Результат (вставить output):**

```
<заполнить SQL output>
```

### Шаг 6 — Verify-criteria (DoD smoke)

Все пункты должны быть `true`:

- [ ] В `users` ровно 1 строка с `telegram_id = '<твой-tg-id>'`, `display_name` непустой.
- [ ] В `conversations` ровно 1 строка для этого `user_id`, `channel='telegram'`, `handoff_mode='ai'`, `assigned_operator_id IS NULL`.
- [ ] В `messages` для этого `conversation_id` — 4 строки в порядке: user `/start`, assistant GREETING, user `«Привет...»`, assistant AI-reply.
- [ ] У assistant-сообщений в `meta` есть `usage.input_tokens` / `usage.output_tokens` (для AI-reply) либо `source: 'static_greeting'` (для GREETING).
- [ ] У user-сообщений в `meta` есть `telegram_update_id` и `telegram_message_id`.

### Шаг 7 — Negative-check (idempotency-ish)

```sql
DELETE FROM users WHERE telegram_id = '<твой-tg-id>';  -- CASCADE → conversations + messages
```

В TG `/start` снова → ожидание: новые строки в `users` / `conversations` / `messages`. Подтверждает upsert path и graceful re-creation.

### Шаг 8 — Vercel Logs

В Vercel Dashboard → Project → Logs (Preview) проверить наличие событий:

- `telegram.persist.start` / `telegram.persist.done`
- `db.users.created` / `db.conversations.created` / `db.messages.persisted` (×4)
- p50 / p95 webhook latency: `<заполнить>`

## Метрики

| Метрика | Значение | Источник |
|---|---|---|
| webhook end-to-end (start, p50) | | Vercel Logs `telegram.persist.done` |
| webhook end-to-end (текст, p95) | | Vercel Logs `telegram.message.ai_reply` |
| db upsert duration (`db.users.upsert.done`) | | Vercel Logs |
| AI round-trip (`durationMs`) | | Vercel Logs |
| Persist failures (Sentry) | | Sentry → tag `source=telegram.persist` |

## Открытые вопросы / next milestone

- Идемпотентность Telegram update_id — нужен UNIQUE-constraint на `messages(meta->>'telegram_update_id')` или отдельная колонка. Разгребём в milestone «Расширение схемы БД» вместе с `payments(provider, provider_ref)`.
- Загрузка истории диалога в AI-context — milestone «State machine + AI tools». До него `runAgentNoTools` остаётся stateless.
- Custom-домен для production — отложен.
- Sentry-алерты на `telegram.persist.failed` (ошибки БД) — milestone «Production-ready».
