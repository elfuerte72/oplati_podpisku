name: Security

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  dependency-audit:
    name: Dependency Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: pnpm/action-setup@v4
        with:
          version: 9.12.0
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Fail only on HIGH/CRITICAL; adjust when dedicated SLA is defined.
      - name: Audit production dependencies
        run: pnpm audit --prod --audit-level=high

  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high
          # Гейтим только production (runtime) зависимости — консистентно с
          # job'ом dependency-audit выше (`pnpm audit --prod`). Dev-инструменты
          # (vitest, drizzle-kit и т.п.) не попадают в прод-бандл; их advisory
          # отслеживаем, но они не блокируют merge. Прод-уязвимости по-прежнему
          # валят PR (fail-on-severity: high).
          fail-on-scopes: runtime
          comment-summary-in-pr: on-failure
