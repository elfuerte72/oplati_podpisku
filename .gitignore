node_modules/
.next/
dist/
.turbo/
.vercel/
.env
.env.local
.env.*.local
*.log
.DS_Store
coverage/
.drizzle/

# macOS screenshots — попадают в корень при Cmd+Shift+4 / 5
Screenshot*.png
Screenshot*.jpg
Screenshot*.jpeg

# Локальные артефакты smoke-тестов (туннели, dev-логи)
.smoke/

# Локальные справочные/рабочие артефакты (не код проекта)
api-1.json
content_site/
.agents/
.codex/
skills-lock.json

# Справочные материалы (PRD, TZ, customer Q&A, продуктовые .docx/.pdf)
# хранятся локально и/или в .ai-factory/, в git их не пускаем.
# Паттерны привязаны к корню репо (ведущий `/`) — внутри `docs/` они НЕ игнорируются.
/*.docx
/*.pdf
/TZ.md

# Локальный webhook-URL Sentry-алертов (содержит секрет, не коммитить)
.sentry-webhook.local.txt

# Тикеты/спеки скиллов (mattpocock-skills, локальный трекер).
# Репозиторий публичный, тикеты несут рабочие детали контура оплаты — наружу не пускаем.
# Обоснование и как отменить — docs/agents/issue-tracker.md
.scratch/

# Локальные настройки Claude Code — внутри env-секреты (DOKPLOY_API_KEY и пр.).
# Глобальный ~/.config/git/ignore их тоже ловит, но он не переезжает с репозиторием.
.claude/settings.local.json
