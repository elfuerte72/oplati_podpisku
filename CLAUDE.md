# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**Источник правды — код + этот файл.** Если документ противоречит коду — прав код.
Карта всей документации (что авторитетно, что архив, как её вести) —
[`docs/README.md`](docs/README.md). Коротко:

- **Актуальное:** [`docs/architecture.md`](docs/architecture.md) (устройство кодовой базы), [`docs/BACKLOG.md`](docs/BACKLOG.md) (**единственный** список «на потом»: отложенное, открытые находки аудитов, идеи), [`docs/CHANGELOG.md`](docs/CHANGELOG.md) (история изменений — вести обязательно), [`docs/incidents.md`](docs/incidents.md) (журнал инцидентов — новые фиксировать там).
- **Рунбуки** (`docs/runbooks/`): [`deploy.md`](docs/runbooks/deploy.md) (пайплайн prod/dev + контракт deploy-вебхука Dokploy + смоук), [`backup-restore.md`](docs/runbooks/backup-restore.md) (бэкапы в R2, учение по восстановлению), [`rollback.md`](docs/runbooks/rollback.md) (откат и гашение резерва), [`monitoring.md`](docs/runbooks/monitoring.md) (Grafana Cloud: логи узла в Loki, внешний uptime, алёрты в Telegram; Sentry), [`server-migration.md`](docs/runbooks/server-migration.md) (переезд контура на другой VPS: что чем переносится, порядок окна, грабли), [`vcc-funding.md`](docs/runbooks/vcc-funding.md) (пополнение карточного фонда: нормы остатка, сигналы, что делать при отказе клиенту), [`metabase.md`](docs/runbooks/metabase.md) (Metabase рядом с боевой БД: ssh-туннель, read-only роль `metabase_ro`, выдача доступа к новой таблице).
- **Справочники** (`docs/reference/`) — подробности подсистем, вынесенные из этого файла; в нём остались только инварианты: [`payment-gateways.md`](docs/reference/payment-gateways.md) (приём рублей: L&P + Freekassa) и [`freekassa-api.md`](docs/reference/freekassa-api.md) (контракт, снятый живым платежом) + [`loveandpay-api-access.md`](docs/reference/loveandpay-api-access.md) (домен и IP-allowlist), [`referral-program.md`](docs/reference/referral-program.md) (партнёрская программа), [`client-path.md`](docs/reference/client-path.md) (путь клиента и Mini App-кабинет), [`analytics.md`](docs/reference/analytics.md) (поведенческая аналитика), [`remnawave-api.md`](docs/reference/remnawave-api.md) (VPN: контракт панели + кнопка в боте), [`testing.md`](docs/reference/testing.md) (что покрыто тестами и какой инцидент это породил), [`virtual-cards.md`](docs/reference/virtual-cards.md) (карты PaySpace), [`infrastructure.md`](docs/reference/infrastructure.md) (VPS, Dokploy, деплой), [`env-vars.md`](docs/reference/env-vars.md) (все переменные окружения), [`ai-cost-protection.md`](docs/reference/ai-cost-protection.md) (слои защиты AI-расходов), [`database.html`](docs/reference/database.html) (как работает БД; инфраструктурная часть — эпохи Supabase).
- **Архив** (`docs/history/`) — исполненные планы, закрытые аудиты, устаревшие спеки. **Не удалять** (история решений) и **не использовать как ТЗ**: каждый файл описывает контур, которого уже нет.

Если поведение не очевидно из кода — спросите владельца, не додумывайте.

## О проекте

Telegram-бот + веб-чат для оплаты иностранных подписок русскоязычным пользователям. Клиент пишет, что хочет оплатить, — AI-агент «Оплатишка» находит цену, создаёт заказ, принимает оплату в RUB; исполнение пока ручное (операторы). Масштаб старта — ~50 заказов/день.

**Стек:** TypeScript 5.6 · Node.js 24 · pnpm + Turborepo · Next.js 16 (App Router) · grammY · `@anthropic-ai/sdk` (свой tool-loop, default-модель `claude-sonnet-4-6`, override через `ANTHROPIC_MODEL`) · self-hosted Postgres 17 (переезд с Supabase 2026-07-24; Storage/Auth/Realtime не используются) · Drizzle · Zod · Tailwind v4 · Sentry + pino · Vitest · **Dokploy на VPS** `187.124.172.104` (Docker + Traefik) + системный crontab.

**Осознанно НЕ используем** (не предлагать без явного запроса): Vercel AI SDK и токен-стриминг (чат целевой, короткие ответы; свой tool-loop работает в проде), Trigger.dev (хватает системного crontab; env зарезервирован), shadcn/ui (свой комикс-UI).

## Что работает сейчас

- **Telegram-бот → `/api/bot`** — grammY webhook (проверка `X-Telegram-Bot-Api-Secret-Token`, единственный non-200 кейс → `401`). **Дедуп по `update_id`** (`lib/dedup.ts`, аудит 2026-08-10): Telegram переДОСТАВЛЯЕТ апдейт, если не получил 200 вовремя, а обработчик синхронный и живёт до 90 с — повтор проходил весь путь заново (второе сообщение, второй счёт по `confirm`, второй вызов панели VPN). Ключ в Redis несёт id бота (иначе прод и dev гасят апдейты друг друга) и живёт В ДВЕ ФАЗЫ: 100 с на время работы, 10 мин после неё — умерший процесс ключ не освобождает, и длинный TTL с самого начала превращал бы ретрай Telegram в тихую потерю. Fail-open и быстро (ретраи клиента выключены, предел ожидания 1,5 с): claim идёт ПЕРЕД обработчиком, и зависший Redis иначе остановил бы весь бот. ⚠️ **`ANTHROPIC_API_KEY` в жёсткий гейт роута НЕ входит** — его пропажа глушила `/start`, меню, оплату и VPN, к Anthropic отношения не имеющие; гейт живёт на AI-пути (`runAgentDialog`), где отвечает текстом со ссылкой на `/support` и шлёт Sentry-алёрт. **Взаимодействие с Оплатишкой в ЧАТЕ бота — за флагом `BOT_AI_ENABLED` (дефолт ВЫКЛЮЧЕНО, 2026-07-03):** при выключенном флаге бот не обслуживает текст/медиа/каталожные inline-кнопки/`/menu` — работают только команды `/start`, `/support` и inline-меню, а покупки идут в Mini App. **Молчания больше нет** (тикет 09 админ-панели, 2026-08-17): на свободный текст и на медиа уходит одна фраза с кнопкой «Поддержка» — раньше написавший «помогите» не получал НИЧЕГО и уходил. Обращение по-прежнему создаётся ТОЛЬКО нажатием кнопки (правило владельца), подсказка его не создаёт. Дедуп в ДВА эшелона (`lib/telegram/silent-hint.ts`): Redis на разные процессы и память процесса на альбом — Telegram шлёт апдейт на каждое фото, а `claimOnce` fail-open, и без локального эшелона десять фото давали бы десять подсказок там, где Redis не настроен; при `BOT_AI_ENABLED=1` — обычный путь `runAgent()` из `@oplati/agent` → Anthropic → tools → Supabase (AI-диалог + кнопочный `/menu`-каталог). Код обоих путей сохранён — флаг это временный выключатель, не удаление. Диалог синхронно пишется в БД с graceful degradation при её недоступности (бот отвечает, но «забывает» историю).
- **Веб-чат → `/api/chat`** — тот же `runAgent()` (инвариант: оба канала используют одного агента), ответ JSON-ом без стриминга. **AI-диалог на сайте — за флагом `WEB_AI_ENABLED` (дефолт ВЫКЛЮЧЕНО, решение владельца 2026-07-19):** при выключенном флаге любое сообщение в поле ввода получает мгновенную заготовку «выбери сервис в каталоге / поддержка в боте» — без вызова Anthropic, без сессии и записей в БД (rate-limit тоже не зовётся — ответ статический); UI сайта не меняется, покупка кнопочная (`/api/orders/propose`+`confirm`) не затронута. Код агента цел — временный выключатель по образцу `BOT_AI_ENABLED`. История — `/api/chat/history`, сброс — `/api/chat/clear`, сессия по cookie (`lib/chat/session.ts`). Конвертация истории в Anthropic-формат — ОБЩИЙ `toAgentHistory` (`lib/chat/history.ts`, дубль из handle-update.ts удалён 2026-07-18): отрезает ведущие `assistant` — окно `loadRecentMessages(…, 20)` режет по числу строк и при непарной записи начиналось с assistant → Messages API отвечал 400 на каждый ход НАВСЕГДА (чётность окна сохраняется; H-1 аудита). Комикс-UI «Оплатишка» (`components/chat/`, `components/comic/`, skill `oplatishka-design`). Интро при первом визите — `components/intro/IntroOverlay.tsx` (2 кадра, localStorage-флаг).
- **Привязка Telegram к веб-сессии** — `POST /api/auth/telegram/link` выпускает одноразовый токен (TTL 10 мин, таблица `link_tokens`) → deep-link `telegram.me/<bot>?start=link_<token>` → бот в `/start link_*` зовёт `consumeLinkToken` (если у клиента есть и telegram-, и веб-строка в `users` — merge в одной транзакции, выживает telegram-строка, children переезжают) → клиент поллит `GET /api/auth/telegram/link/status`. Точки входа UI: интро, панель профиля, карточка-гейт в чате (`components/chat/TelegramLink.tsx`). **После привязки** панель профиля (`ProfilePanel.tsx`) вместо кнопки Telegram показывает **«Личный кабинет»** — ссылку на Mini App (`cabinetUrl` из `/api/profile` ← `cabinetDeepLink()`: задан `TELEGRAM_MINIAPP_SHORTNAME` → прямой `telegram.me/<bot>/<shortname>`, иначе fallback `telegram.me/<bot>?start=cabinet` через /start-меню с web_app-кнопкой; веб-браузер сам открыть web_app-кнопку не может). Аватар в профиле осознанно НЕ показываем (у веб-сессии нет `initData`; тянуть фото через Bot API `getUserProfilePhotos` + прокси ради кружка не стали) — только имя из Telegram. **Механика перехода (фикс мобильной привязки 2026-07-03):** токен выпускается ЗАРАНЕЕ при рендере кнопки (prefetch с гейтом видимости; перевыпуск по TTL; rate-limit `web-link` поднят 5→10/мин), кнопка — настоящая `<a href="telegram.me/...">`: universal link открывает приложение Telegram только при прямом тапе; прежняя схема `window.open('about:blank')` + `location.replace` после `await` на iOS/in-app-браузерах не доводила пользователя до бота (токены выпускались, но не потреблялись — видно в `link_tokens.used_at IS NULL`). При `waiting` показывается fallback «скопировать ссылку». **Handoff заказа в бота:** после успешного `consumeLinkToken` бот ищет свежий (≤24 ч) заказ `ready_for_payment` и, если нашёл, сразу выставляет счёт (`confirmOrder`) и шлёт ссылку оплаты в чат — возвращаться в мобильный браузер не нужно; сбой handoff не роняет привязку (обычный успех-текст). `payments/create` при повторном confirm заказа в `pending_payment` идемпотентно возвращает существующий pending-инвойс (`repeat_confirm`), поэтому живая веб-вкладка, повторившая подтверждение после привязки, получает ту же ссылку. **Гейт оплаты:** веб-пользователь без `telegram_id` не получает платёжную ссылку — `confirm_order` бросает `TelegramLinkRequiredError` (`telegram_link_required`), т.к. чек и реквизиты карт доставляются только в Telegram. **Мобильный сайт — воронка в Telegram:** баннер «Продолжить в Telegram» (`components/chat/MobileTelegramBanner.tsx`, только <lg, прямая `<a>` на `supportUrl` из `/api/profile`); анти-петля — кнопка «Сайт» в /start-меню бота ведёт на `/?src=tg`, такой визит помечается в sessionStorage и баннер не показывается.
- **Tools агента** (`packages/agent/src/tools.ts`): `web_search` (серверный tool Anthropic — актуальные цены, в каталоге цен НЕТ), `search_catalog`, `propose_order`, `confirm_order`, `request_human`. Заказ принимается на **любой** сервис: из каталога (`serviceId`) или вне его (`customDescription` — свободный текст, оператор перепроверит). Каталог `services` публичен только на чтение активных записей (`RLS + SELECT policy`), запись — только server-side/service role. **Кнопочный веб-выбор (`StartScreen`)** сейчас без пункта «Свой вариант…» — флаг `ALLOW_OWN_VARIANT=false` (2026-07-02): временно ограничиваем список доступными к оплате сервисами (часть карт/подписок не принимаем); код сохранён, AI-путь `customDescription` не затронут. Цена = `курс × USD-цена + COMMISSION_PERCENT` (на проде 30%, дефолт 10%) + разовая **надбавка за выпуск карты** `CARD_ISSUE_FEE_USD_CENTS` (на проде $4=400, дефолт 0) — берётся только когда у клиента нет активной карты (`findActiveByUserId`; при повторной оплате топап без fee); снимок в `orders.card_issue_fee_kopecks` (миграция 0019), экран заказа показывает разбивку «Подписка / Выпуск карты / Итого». **Копеек в цене нет: подписка и надбавка округляются ВВЕРХ до целого рубля** (`roundUpToWholeRubles` в `apps/web/lib/pricing.ts`, решение владельца 2026-08-05). Округляются именно эти две строки, а не итог: витрина каталога показывает `subtotal + commission` (без fee) — округли только итог, и «≈ 868 ₽» в списке тарифов разошлось бы с суммой заказа, а разбивка чека перестала бы сходиться. Разница уходит в комиссию (`amountRubKopecks` в результате tool'а остаётся сырым subtotal). Обе точки расчёта — `lib/catalog/build.ts` (оценка) и `lib/tool-handlers/propose-order.ts` (фиксация) — обязаны звать одну функцию. Курс USD→RUB (внутренне `usdt_rub`) берётся из публичного Rapira `GET /open/market/rates`, пара `USDT/RUB`, поле `askPrice` (цена покупки USDT за RUB); при ошибке сети/HTTP/контракта — `RATE_FALLBACK_USDT_RUB` (дефолт 81; поднят с 77 2026-07-19, M-14). Курс фиксируется при `propose_order` **на 2 часа** (`TTL_HOURS`; было 24ч — решение владельца 2026-07-18: суточная фиксация = односторонний опцион на курс за счёт маржи). Фиксация форсится сервером (H-2 аудита): переход `ready_for_payment → expired` разрешён в state machine, протухший черновик хоронит cron `expire-payments` ИЛИ гейт `isPriceLockExpired` в `payments/create` (`409 order_expired` → UI говорит «оформи заново»). **Принимаем ТОЛЬКО рубли** (СБП/карта) — USDT/крипту от клиента НЕ принимаем (убрано из GREETING/SYSTEM_PROMPT 2026-07-03).
- **Приём рублей — два шлюза, Freekassa основной с 2026-07-28** (`PAYMENT_PRIMARY_PROVIDER=freekassa`, касса `74953`; Love&Pay — резерв). **Инварианты, которые нельзя нарушать:** флаг выбирает шлюз ТОЛЬКО для нового счёта (`lib/payments/gateway.ts`), **вебхуки ОБОИХ провайдеров работают всегда** — иначе в момент переключения оплаты по уже выставленным счетам прежнего шлюза не были бы приняты; ответ вебхука — HTTP 200 всегда (инвариант 6), у Freekassa принятым уведомление считается по ТЕЛУ `YES`; идемпотентность — атомарный `claimPaymentSucceeded` (`pending→succeeded` одним условным UPDATE), побочные эффекты только у победителя гонки webhook↔poll; **недоплата терминальна** (`amount_mismatch` → платёж и заказ в `failed` + РОВНО один DM владельцу через `claimPaymentTerminal`); счёт живёт 1 час (`FREEKASSA_INVOICE_TTL_HOURS` / константа `LOVEANDPAY_INVOICE_TTL_HOURS`), `orders.expires_at` выравнивается по сроку инвойса в ТОЙ ЖЕ транзакции. Потолок держится в два эшелона: витринный кап **$1200** и рублёвый гейт `FREEKASSA_MAX_AMOUNT_RUB` (дефолт 140 000). Тех. сбой транспорта → `503 provider_unavailable` и текст «заказ сохранён, попробуй позже» во всех каналах. Всё остальное — базовые URL, формулы подписей, контракт вебхуков, конверсионная метрика, автофоллбэк, комиссия покупателя — в [`docs/reference/payment-gateways.md`](docs/reference/payment-gateways.md); контракт Freekassa, снятый живым платежом, — в [`docs/reference/freekassa-api.md`](docs/reference/freekassa-api.md).
- **Антифрод-трек Freekassa (2026-08-15, тикеты 01-11, спека в `.scratch/freekassa-antifraud/`).** Причина холдов была в нас: провайдер получал IP нашего VPS и несуществующий email `<telegram_id>@telegram.org` — портрет дроп-схемы. Теперь: **(1) правда о плательщике** — счёт уходит с `users.last_seen_ip` (пишется с троттлингом из живых запросов клиента: кабинет/propose/confirm/чат, `rememberClientIp`; fallback на IP VPS алертится) и настоящим `users.email` (обязателен: «плашка контактов» на экране заказа сайта и Mini App — общий `components/contacts/ContactCard.tsx`; гейт `422 email_required` в `payments/create`; суррогат — алертящаяся аномалия); merge при привязке Telegram переносит контакты веб-строки. **(2) Телефон от порога** `PHONE_REQUIRED_FROM_RUB` (env, не задан → выключено; прод 10000): гейт `422 phone_required` с порогом в теле (в UI не зашит — `isPhoneRequiredForAmount` единственное место конверсии; порог отдают `/api/profile` и cabinet-snapshot), сбор — плашка/`request_contact` в боте (чужой контакт отвергается по `contact.user_id`; Mini App «Взять из Telegram»: `requestContact` номер приложению НЕ отдаёт — приходит боту contact-сообщением) + экран «Профиль» (action `update-contacts`; ручная правка → `phone_source='manual'`); `tel` провайдеру — за флагом `FREEKASSA_SEND_TEL` (дефолт ВЫКЛ до живого подтверждения контракта); сверка `phone_match` в meta `payment_succeeded` — только пометка (Р4). Контакты и IP — режим PAN: redact в pino/Sentry (канарейка `lib/contacts/redact-canary.test.ts`). **(3) Видимость холда**: статус `7` = `ANTIFRAUD_HOLD` (эмпирический, подтверждён поддержкой 2026-08-14), каждый опрос пишет `payments.last_provider_status`; холд → заказ в **`payment_review`** («Платёж на проверке банка»: НЕ протухает, `pending_payment → payment_review → paid|failed|cancelled`; poll опрашивает его платёж без 25ч-потолка; дольше 7 дней → DM-сторож `payment-review-watch` без автозакрытия) + клиенту РОВНО одно автосообщение (дедуп по смене `last_provider_status`); исход-отказ → честное сообщение + DM. **(4) Кнопка «Проблема с оплатой»** (до выпуска карты; Mini App + сайт `/api/orders/problem`; «я оплатил» → `payment_review`, дедуп DM час, доставка ПЕРВОЙ) и **учёт возвратов** (`countRefundishHistoryByUser` 180 дней → информ-DM; возврат по succeeded-платежу → денежная аномалия). Миграции 0031-0034 применены на прод 2026-08-15 вместе с выкатом (PR #161, env `PHONE_REQUIRED_FROM_RUB=10000` задан); живой заказ подтвердил: счёт уходит с настоящими email и IP, fallback-алертов ноль.
- **Статус заказа**: `/api/orders/status`, подтверждение — `/api/orders/confirm`.
- **Личный кабинет — Telegram Mini App `/cabinet`** (`components/cabinet/`, бэкенд `POST /api/cabinet`): кнопочный каталог → экран заказа → карта клиента → партнёрка. **Авторизация — проверка подписи `initData` на КАЖДОМ запросе** (`lib/cabinet/auth.ts`); сплошного IP-лимита у роута нет намеренно (инвариант 9). Списка заказов в кабинете осознанно НЕТ (решение владельца 2026-07-02) — снапшот бэкенд отдаёт, UI не рендерит. Экраны, действия, live-баланс карты и правило «Действует до» — [`docs/reference/client-path.md`](docs/reference/client-path.md).
- **VPN Оплатишки — кнопка «🛡 VPN» в /start-меню бота** (Remnawave, панель `panel.mxpkn8ns.ru` на ОТДЕЛЬНОМ сервере). Один telegramId = один юзер панели, повторное нажатие возвращает ту же ссылку (БД → `by-telegram-id` → создание), «Обновить ссылку» = `revoke` и осознанно НЕ продлевает `expireAt` (иначе кнопка = бесплатное продление). `REMNAWAVE_API_TOKEN` — только server-side. Продление по оплате не реализовано. Контракт панели и продуктовый флоу — [`docs/reference/remnawave-api.md`](docs/reference/remnawave-api.md).
- **Реферальная (партнёрская) программа** — одноуровневая, за флагом `REFERRAL_ENABLED` (на проде soft-start). **Инварианты:** ledger `referral_accruals` append-only, идемпотентность `UNIQUE(payment_id, beneficiary, level)`, сумма начислений заказа ≤ его комиссия; ставка платится ЗАФИКСИРОВАННАЯ — `referral_partners.locked_rate_l1_bps` и это ЕДИНСТВЕННЫЙ источник (решение владельца 2026-08-11), расчёт и витрина зовут общую `effectiveReferralRates` с потолком; `referred_by` immutable, установка отклоняется при цикле в дереве; комиссия провалившегося заказа гасится компенсирующей строкой `reversed`. **Выплаты реальных денег НЕ работают** — исполнитель ещё mock, `settlePayout` нигде не вызывается, заявка уходит владельцу в Telegram и обрабатывается вручную. Захват реферера, прогрессия статусов, кабинет партнёра и этапы выплат — [`docs/reference/referral-program.md`](docs/reference/referral-program.md). **UI-термин «статус»; в коде и БД идентификатор остался `circle`/`current_circle`.**
- **Клиентский путь (ТЗ 2026-07-18, PR #81)** — УТП на первом экране, пер-сервисные правила оплаты из `services.payment_instructions` (jsonb, миграция 0022), прозрачная цена (сумма прямо в кнопке оплаты + раскрывашка «Как рассчитана сумма»), пост-выпускные статусы заказа как производные от append-only событий `order_events` (статус-машина НЕ тронута, `completed` терминален). ⚠️ **Страна выпуска карты публично НЕ указывается** («виртуальная карта», не «американская») — проверять при добавлении любых текстов. Экраны и действия — [`docs/reference/client-path.md`](docs/reference/client-path.md).
- **Поведенческая аналитика** — своя таблица `analytics_events` + вьюхи + Metabase. ⚠️ **Денежные вехи телеметрией не дублировать никогда** (`order_created`/`payment_invoice_created`/`payment_succeeded`/`fulfillment_completed`/`subscription_activated`/`payment_issue_reported`/`order_expired` уже в `order_events`, в одной транзакции с деньгами): копия пишется best-effort через `after()` и разойдётся с оригиналом в отчёте о выручке — поэтому вызовов `track()` НЕТ в вебхуках и `issue-card`. `user_id` в событии не хранится (identity канала + JOIN); `name`/`channel` — `text` без FK на словарь. Словарь событий — `packages/types/src/analytics.ts`, устройство и приём — [`docs/reference/analytics.md`](docs/reference/analytics.md).
- **Cron (`infra/crontab.example` → `/etc/cron.d/oplatishka` на VPS → `/api/cron/*` → `lib/jobs/*`; авторизация `Authorization: Bearer <CRON_SECRET>`)**: `poll-payment` (каждые 5 мин: **опрос баланса VCC и запись снимка `vcc_balance_snapshots`** — им живёт preflight оплаты, и его свежесть в 30 минут рассчитана именно на этот шаг; подстраховка от потерянных webhook'ов + recovery зависших в `paid` через `findStuckPaidOrders` → повтор `issue-card`, гейт `isPaySpaceConfigured`; + healthcheck L&P-прокси `lib/jobs/proxy-health.ts` — H-3 аудита, SPOF приёма денег: сетевая ошибка/таймаут CONNECT → Sentry `lnp_proxy_down` + DM владельцу через `notifyOps` с дедупом 60 мин; вне гейта PaySpace. ⚠️ Сейчас спит: `LOVEANDPAY_PROXY_URL` не задан, egress прямой), `expire-payments` (15 мин; хоронит ОБА оплатимых статуса с истёкшим `expires_at` — `pending_payment` И `ready_for_payment`-черновики с протухшей фиксацией цены, `findExpiredPayableOrders`), `renewal-reminder` (07:00, дедуп через order-event `renewal_reminder_sent` — окно выборки шире шага крона, иначе 3-4 дубля), `recycle-cards` (03:30, закрывает карты старше `CARD_LIFETIME_DAYS` от выпуска — `release`+`recycled`, в ЛЮБОМ статусе; шага «active→idle по простою» больше нет, решение владельца 2026-07-25), `keepalive` (в crontab НЕ переносили: это была анти-автопауза Supabase free tier, у self-host Postgres автопаузы нет; роут в коде остался), `referral-recovery` (каждый час — добор пропущенных реферальных начислений), `referral-rollup` (1-е число месяца, 02:00 UTC — месячная прогрессия статусов, гейт `REFERRAL_ENABLED`; при разрыве с последним посчитанным месяцем шлёт лог/Sentry/DM владельцу — ⚠️ САМ пропущенный месяц НЕ считает, автодобор выдавал бы бонусы задним числом в append-only ledger, разбор в BACKLOG), `retention` (04:15 — M-13: `messages` старше 90 дней удаляются, протухшие занятия карточного фонда старше недели убираются, `payments.raw_payload` старше 180 дней очищается батчами; `orders`/`order_events` не трогаются — аудит-след), `support-housekeeping` (каждые 15 мин в `:07` — мимо `expire-payments`; на VPS с 2026-08-28: разговоры у оператора без реплик клиента дольше 24 ч → `idle`, обращения «без ответа» дольше 2 ч в рабочее время → алёрт персоналу с дедупом 4 ч через встроенный фолбэк `notifyStaff`), `funnel` (каждые 15 мин в `:11` — движок воронки обратной связи, см. пункт про воронку; гейт `RETENTION_FUNNEL_ENABLED`). ⚠️ Установленный `/etc/cron.d/oplatishka` отличается от `infra/crontab.example` формой строки: `--resolve www.oplatishka.com:443:127.0.0.1` (мимо DNS, прямо в Traefik) и тег `logger -t oplatishka-cron-<job>` — новую строку брать по образцу соседних в файле, а не копировать из example буквально.
- **Защита AI-расходов (оба канала)**: Haiku-роутер перед агентом (`packages/agent/src/router.ts` — приветствие/оффтоп/инъекция получают каннед-ответ без Sonnet; при сомнении и при ошибке роутера — fail-open в агента; выключатель `AI_ROUTER_DISABLED=1`); дневной глобальный токен-бюджет (`ai_usage_daily` + `apps/web/lib/ai/budget.ts`, env `AI_DAILY_TOKEN_BUDGET`, взвешенные токены, fail-open при недоступной БД, Sentry-алерт на пересечении порога); серверные границы в `propose_order` ($1–500; исключение — каталожные «пополнения» Airbnb/Booking/Steam/App Store: до **$1200** строго по slug из `HIGH_VALUE_SERVICE_SLUGS` — потолок опущен с $5000 2026-07-28 под лимит операции Freekassa 150 000 ₽; **зеркало** (инвариант 10): то же значение живёт в `lib/telegram/amount.ts`, `StartScreen.tsx`, `CatalogView.tsx`; custom-описания остаются на $500; ≤10 заказов/сутки на пользователя; VCC-preflight перед выставлением счёта с 2026-08-25 ЕСТЬ — см. фазу 2 ниже); per-identity rate-limit (`apps/web/lib/ratelimit.ts`, Upstash sliding window) — `/api/chat` по IP, `/api/bot` по `telegram_id`, ДО роутера. У бота с 2026-08-11 ТРИ бакета, по одному на вход: `telegram-start` (`/start`, включая `link_`/`ref_` — это шаг оплаты, его нельзя ронять чужим трафиком), `telegram-media` (Telegram шлёт апдейт на КАЖДОЕ фото альбома, общий бакет альбом бы и выел — вместе с кнопкой «Поддержка»), `telegram` (текст и callback-кнопки). У Mini App свой `cabinet` — общий с ботом означал, что листание кабинета выедает лимит бота; env `UPSTASH_REDIS_REST_*` (на проде — self-host Redis + SRH на том же VPS) ИЛИ легаси-имена `KV_REST_API_*`, не заданы → fail-open, выключатель `RATE_LIMIT_DISABLED=1`.
- **Поддержка — AI-помощник + оператор (трек support-ai, 2026-08-27, спека `.scratch/support-ai/`, ADR 0002).** У разговора есть **режим** `conversations.handoff_mode` — кто сейчас отвечает клиенту: `idle` (никто; свободный текст получает подсказку с кнопкой), `ai` (сессия помощника), `operator` (человек; помощник молчит). Плюс `mode_expires_at` — когда режим сам вернётся в `idle`. **Инварианты:** (1) **бот ЧИТАЕТ режим** — включи AI, и он ответит поверх человека, если этого не делать; (2) **все переходы — через одну функцию `transitionConversationMode`** (`packages/db/src/repositories/support.ts`, условный UPDATE как у заказов; ноль строк = переход не состоялся, вызывающий получает ФАКТИЧЕСКОЕ состояние, а не запрошенное) и каждый пишет служебную строку `role='system'` в той же транзакции — панель её видит, клиент нет, помощник в историю не получает; (3) **вход в сессию помощника — ТОЛЬКО кнопкой «Поддержка», `/support`, deep-link `?start=support`** (правило В3): свободный текст обращения не создаёт; (4) **денежных действий у помощника нет** — tools только читают (`get_my_orders`, `get_service_instructions`, `search_catalog`, `request_human`), и **PAN не выдаётся** — третьего канала выдачи реквизитов не будет; (5) **срок гаснет ЛЕНИВО** — истёкшую сессию замечает следующее входящее, и в БД у неё по-прежнему `ai`: открытие и закрытие ОБЯЗАНЫ это учитывать (переход с `from: 'idle'` не совпал бы ни с одной строкой, и первое нажатие после получаса тишины не отвечало бы ничего); (6) **неотвеченное обращение не гаснет никогда** — `mode_expires_at IS NULL` в `operator` означает «ждём человека», и это же обязан ставить захват из панели («подключиться» — не значит «ответил»). **Помощник** — `deepseek-v4-flash` через Anthropic-совместимый endpoint и тот же `@anthropic-ai/sdk` (профиль движка `AgentProfile`, `packages/agent/src/run.ts`; продажный агент и Haiku-роутер остаются на Anthropic, расходы DeepSeek в дневной бюджет НЕ входят), за флагом `SUPPORT_AI_ENABLED` (дефолт ВЫКЛ — тогда сегодняшний двухшаговый флоу к человеку). ⚠️ **Флаг включён без ключа = как выключенный**, а НЕ эскалация: эскалация запирает разговор в режиме оператора, из которого клиента выводит только человек, — одна забытая переменная делала бы всех недостижимыми для бота. Модуль поддержки — `apps/web/lib/support/` (`session.ts` — единственная точка обработки хода, пять портов в `ports.ts`; **порты тотальные**: `model.reply` отдаёт `null` при аварии, `history` — пустой массив; ошибку логирует и шлёт в Sentry адаптер, в модуле нет ни одного `catch`). Текст клиента и история уходят провайдеру **замаскированными** (карты, телефоны, почта — `maskForModel` в `mask.ts`; персоналу в DM — ТОЛЬКО карты, `maskForStaff`: телефон и почта оператору нужны для сверки платежа), `metadata.user_id` — хэш, не telegram_id; контекст чистится от `/start`, приветствия Оплатишки и подсказок (маскот на «ты» спорил бы с системным текстом помощника). Помощник — на «вы», без эмодзи, **закрытый мир**: база знаний в `packages/agent/src/support-prompt.ts` (экспортируется ОТДЕЛЬНО от промпта — канареечный тест денилиста идёт по базе, потому что промпт обязан называть запретные темы, чтобы их запретить), динамика — `SupportFacts` из env/констант ТАМ, где ими пользуется код (зеркал нет: `PRICE_LOCK_TTL_HOURS` экспортирован из `propose-order`, срок счёта — `currentInvoiceTtlHours()`). **Эскалация** — четыре источника, один путь `escalate()`: жёсткий триггер (`hard-triggers.ts`, ДО модели, до ключа и до капа — «верните деньги» должно попасть к человеку независимо от доступности модели; отрицание срабатывает намеренно; «отзыв» не триггер), tool `request_human` (хук `SupportModelHooks`, тот же переход), выходной фильтр (`output-guard.ts`, ПОСЛЕ модели — гарантия там, где промпт лишь просьба; пойманный ответ клиенту не уходит и в переписку не пишется), сбой модели. Продажный `request_human` переведён на тот же механизм через колбэк `escalateToHuman` в `ToolContext` (колбэк, не импорт: веб-чат не должен грузить бота). Сообщение клиента в режиме оператора — маркер `support_request` на его строке (панель считает «без ответа» от ПОСЛЕДНЕГО), срок → `null`, персоналу пинг с дедупом 30 мин по `messages.meta.staff_pinged_at` (в БД, не в Redis: у Redis fail-open). Кап — 100 ответов на клиента в сутки по БД, без авто-эскалации (решение владельца). ⚠️ Регэкспы: `\b` в JS не видит кириллицу — у русских основ границ нет; страна выпуска ловится в окне ±40 символов от «карт» (сервис может быть американским, VPN-локация из инструкции разрешена). **Панель** (`/admin/support`): ответ = **захват** (`from: [idle, ai, operator]`, `onlyIfFreeOrOwnedBy`, срок 24 ч; чужой → 409), клиенту с префиксом «Оператор: », в БД без (префикс — свойство доставки); «Вернуть помощнику» (`operator → ai`, ведущему и админу — `canReturnToAi`) и «Закрыть» (`operator → idle`, чужой закрыть МОЖНО — это способ завершить, не перехватить); лента отличает «Помощник» от «Бота» по `meta.source` (в БД оба `assistant`) и показывает переходы серыми служебными строками с триггером. **Крон `support-housekeeping`** (15 мин): автозакрытие `operator` с истёкшим сроком (24 ч после ответа оператора) + алёрт «без ответа > 2 ч» в рабочее время с дедупом 4 ч (пустой штат → владельцу). Старые тексты флоу поддержки переведены на «вы»; `GREETING`, VPN, контакты, оплата не тронуты. Eval — `pnpm --filter @oplati/agent eval:support` (живой ключ, ~41 кейс, exit≠0 при провалах). Форум-топики как целевая схема ЗАКРЫТЫ этим решением.
- **Воронка обратной связи (трек retention-funnel, 2026-09-01, спека `.scratch/retention-funnel/`)** — за флагом `RETENTION_FUNNEL_ENABLED` (дефолт ВЫКЛ; **на проде ВКЛЮЧЕНА 2026-09-01** вместе с `REVIEWS_CHAT_URL=https://t.me/oplatishka1` после смоука на dev; миграция 0042 применена на прод+dev, крон-строка `funnel` стоит на VPS, `metabase_ro` имеет SELECT на обе таблицы). Четыре исходящих касания маскота: опрос через 3 ч после протухшего заказа, «нашёл, что искал?» через сутки после `/start` без заказа, оценка 1–5 через час после выдачи карты, реферальное касание через 2 дня после оценки ≥4 (гейт `REFERRAL_ENABLED`; ссылка — тем же кодом, что в кабинете, ставка цифрой не называется). **Инварианты:** (1) единственная точка отправки — привратник `sendFunnelMessage` (`lib/funnel/gate.ts`): флаг → telegram-идентичность → не отписан (`users.funnel_opt_out_at`, кнопка `fb:optout` под каждым сообщением) → нет разговора в режиме оператора (лениво истёкший срок НЕ блокирует) → тихое окно 22:00–10:00 МСК (инверсия `isWithinOperatorHours`, зеркала границ нет) → бюджет ≤1/сутки и ≤3/неделя → у `order_rating` повтор не раньше 90 дней → **атомарный claim ДО отправки** (INSERT в `funnel_sends` с частичными UNIQUE: одноразовые kind'ы по `(user_id, kind)`, оценка по `order_id`); (2) дедуп держит claim, а НЕ выборка — окна выборок шире шага крона и одновременно дают но-бэкфилл (событие старше окна не рассылается никогда — включение флага не трогает существующую базу); (3) время входа заказа в статус — из `order_events`, не из полей заказа; (4) 403 «клиент заблокировал бота» фиксируется и не ретраится, claim остаётся занятым; (5) ответы на кнопки (`fb:*` в диспетчере, работает при выключенном `BOT_AI_ENABLED`) — реакция на действие клиента, в бюджет не входят, повторное нажатие не дублирует запись (`client_feedback`, первый клик побеждает); (6) «Другое» и оценка 1–3 ведут в существующую дверь поддержки (`support-entry.ts`, правило В3 — обращение создаётся только кнопкой), при 1–3 персоналу уходит DM через `notifyStaff` ПОСЛЕ ответа клиенту; SQL воронки — только `packages/db/src/repositories/funnel.ts`; **тексты — дефолты в `templates.ts`, переопределения владельца в БД (`funnel_texts`, панель `/admin/texts`), чтение ОДНОЙ точкой `lib/funnel/texts.ts`** (`getFunnelTexts` с памяткой 60 с и дефолтами при недоступной БД + `renderFunnelText`; ⚠️ строка из БД проверяется НА ЧТЕНИИ (`validateFunnelTextShape`) и негодная откатывается на дефолт с алёртом: `renderFunnelText` бросает на неизвестной `{подстановке}`, а бросает он уже ПОСЛЕ занятого claim'а — касание терялось бы навсегда; длина валидируется по ОТРЕНДЕРЕННОМУ тексту (запас на `{service}`/`{link}`), иначе Telegram отвергает сообщение там же, после claim'а; крон и колбэки констант из `templates.ts` не импортируют — канарейка `texts.test.ts`; редактируются формулировки, числа/сроки/деньги живут только в коде и env, подстановки `{service}`/`{link}` заполняет код). Кнопка «Отзывы» в /start-меню — url из `REVIEWS_CHAT_URL` (не задан → кнопки нет).
- **Админ-панель `/admin`** (внутренняя, спека в `.scratch/admin-panel/`; тикеты 01-13 закрыты). **Живёт на проде с 2026-08-19: `admin.oplatishka.com`**, вход проверен. Живёт ВНУТРИ `apps/web` (кросс-импорты между `apps/*` запрещены, а панели нужны `lib/telegram`, `lib/pay-space`, `lib/alerts`): страницы `app/admin/`, операции `app/api/panel/`, ядро `lib/panel/`. **Вход — Telegram Login Widget + TOTP, паролей нет.** Первый фактор проверяется подписью Telegram по токену ОТДЕЛЬНОГО бота персонала `@oplatishkaasupport_bot` (утечка клиентского токена не должна отдавать вход персонала; точка приёма — свой `/api/staff-bot` со своим secret-token). **Инварианты:** сессия — подписанная httpOnly-cookie без таблицы сессий (на проде с префиксом `__Host-`), а отзыв доступа держится на `staff.is_active`, который перечитывается из БД на КАЖДОМ запросе; назначение токена (`session` / `pending`) лежит ВНУТРИ подписи — иначе промежуточный токен «первый фактор пройден» подкладывался бы в cookie сессии и обходил TOTP; наружу из ядра уходит `PanelActor` без `totp_secret` (строка `staff` несёт второй фактор); неизвестный сотрудник и отключённый получают ОДИНАКОВЫЙ отказ; **код TOTP одноразовый** (`staff.totp_last_step`, атомарный claim окна до выдачи доступа — иначе подсмотренные шесть цифр живут ещё полторы минуты и дают 12-часовую сессию), а подтверждение привязки — compare-and-set по секрету (две вкладки иначе подтверждали секрет, которым никто владения не доказал); ссылка виджета одноразовая (повтор перевыдавал неподтверждённый секрет и убивал запись в приложении сотрудника); **перебор режется ДВУМЯ счётчиками**: по сотруднику (`admin-totp`) с расходом на КАЖДУЮ попытку — при учёте одних промахов пачка параллельных запросов успевает проверить тысячу кодов, а угаданный код проходит мимо лимитера вовсе, — и по IP (`admin-auth`) только на промахах, чтобы чужой флуд с CGNAT-адреса не запирал живого сотрудника; сравнение подписей — через общий `timingSafeEqualStr` (проверка длины в символах при побайтовом `timingSafeEqual` роняла вход в 500 мимо счётчика попыток). ⚠️ **Счётчик попыток на шаге кода — fail-CLOSED**, в отличие от клиентских путей: он ЕДИНСТВЕННЫЙ барьер перебора второго фактора (`pending`-токен живёт 10 минут и переиспользуется), поэтому недоступный Redis закрывает вход, а осознанное отключение флагом (dev) — нет; гейта по IP там НЕТ намеренно — человек уже опознан подписанным токеном, а блокировка по адресу запирала бы живого сотрудника за чужой флуд с общего CGNAT. Cookie панели гасятся ПЕРЕЗАПИСЬЮ с теми же атрибутами, а не `delete` (тот шлёт `Set-Cookie` без `Secure`, и `__Host-`-cookie браузер отвергает целиком — «Выйти» был no-op на проде при рабочем dev), а денилист Sentry чистит `request.cookies` ОТДЕЛЬНО от заголовка (интеграция requestData разбирает его в своё поле до `beforeSend`, и токен сессии уезжал во внешний сервис). Домен — `admin.oplatishka.com` (`infra/traefik/oplatishka-admin.yml.example`; ОТДЕЛЬНОГО приложения в Dokploy у панели нет — Traefik ведёт её домен на тот же сервис `oplatishka-web`), 404 на публичных доменах даёт сам app (решение — `lib/panel/host.ts`, применяют `proxy.ts` и гейт в `lib/panel/session.ts`), а не только Traefik, и гейт **fail-closed на проде**: незаданный `PANEL_HOST` закрывает панель, а не выставляет её наружу (прецедент `CLIENT_IP_MODE`). ⚠️ **Кнопка входа не появится, пока домен не привязан у @BotFather (`/setdomain`)** — и об этом не скажет НИЧТО: скрипт виджета грузится, CSP пропускает, логи и консоль чисты, а причину видно только со стороны Telegram (`oauth.telegram.org/embed/<bot>?origin=…` → `Bot domain invalid`); метода Bot API для привязки нет, только вручную. Заведение сотрудника — скрипт `pnpm --filter @oplati/db db:staff`, не форма; секрет TOTP скрипт НЕ выдаёт (его генерирует панель при первом входе). **Права** (`lib/panel/permissions.ts`): `admin` и `operator`, таблица — `Record<StaffRole, …>`, чтобы новая роль не «падала в менеджера»; проверка живёт В ОПЕРАЦИИ (`guardPanelOperation`) и в странице (`panelPageAccess` — размеченное объединение, где данные достаются только из разрешённой ветки: прежний `{ actor, allowed }` страницы игнорировали и отдавали заказы тому, кому раздел закрыт). Разделы владельца видны менеджеру и отдают заглушку. **Экран заказов** (тикет 03): выборки в `packages/db/src/repositories/panel.ts` (своих SQL в панели нет), фильтры/сортировка/страница — в АДРЕСЕ, потолок страницы и его кламп живут в репозитории, усечение проговаривается вслух («есть ещё» считается выборкой на строку больше; у холдов ещё и «упёрлись в потолок SQL» — запас в три платежа на заказ может не покрыть длинную историю перевыставлений). ⚠️ Поиск кладёт email и телефон клиента в `?q=` — поэтому денилист Sentry чистит и `query_string`, и `request.url`. **Клиент, проверка платежей, ручная выдача** (тикеты 04-06): карточка `/admin/clients/<id>` (итоги считаются В БАЗЕ по `PURCHASED_ORDER_STATUSES`, а не складыванием видимого среза), `/admin/holds` — раздел «Проверка платежей» (заказы `payment_review` + платежи с холдом/отказом). **Остаток карточного счёта с редизайна 2026-08-27 показывается ТОЛЬКО на рабочем столе `/admin`** (`lib/panel/vcc-balance.ts`: читается со СВОИМ коротким дедлайном и кэшируется на минуту — дефолт клиента PaySpace в 60 с на фазу и два захода держал бы страницу до четырёх минут, а живое обновление раз в 25 с копило бы висяки и топило Sentry отказами; недоступный провайдер до 30 минут показывает ПРЕЖНЕЕ число с пометкой «PaySpace не отвечает», старше — «баланс не получен»; порог — `vccAlertThresholdsUsdCents().critical`, та же функция, что питает алёрт, а не прямое чтение env). Ручное исполнение `failed → in_fulfillment → completed` двумя шагами с обязательным комментарием и автором в append-only журнале. **Инварианты этой части:** выдать вручную можно только заказ с УСПЕШНЫМ платежом (`failed` — не синоним «деньги получены»: туда же попадают отвергнутый счёт и недоплата); **«выдал» доступен только заказу, который взяли в работу РУКАМИ** (`isStartedManually` по последнему входу в `in_fulfillment`, одно правило на экран и на операцию): в тот же статус уводит автомат, и отметка «выдал» из-под работающего `issueCard` делала заказ терминально `completed` — упавший следом `markOrderFailed` вернуть его в `failed` уже не может, ошибка гаснет в Sentry, а клиент остаётся без карты при списанных деньгах; мутирующая операция панели сверяет `Origin` и требует `application/json` (`sameSite=lax` между `www` и `admin` не защищает — это один site); «Клиент уведомлён» на экране проверки платежей — ФАКТ доставки из `order_events` (`payment_review_client_notified`), а не вывод из статусов, потому что отправка best-effort и её отказ гасится логом (⚠️ служебные события в таймлайн КЛИЕНТА не показываются — денилист в `lib/cabinet/read.ts`: `order_events` читают трое, и про Mini App забывают); дедуп холда гасится ФАКТОМ (заказ доехал до `payment_review`), а не снимком статуса — иначе сорвавшийся переход хоронил заказ в `expired` вместе с деньгами на холде; алёрт `findStuckInFulfillmentOrders` мерит время ВХОДА В СТАТУС из `order_events`, а не `paid_at` (иначе ручная выдача старого заказа бьёт в Sentry каждые 5 минут). ⚠️ Реферальная комиссия после ручной выдачи НЕ восстанавливается — доказано тестом, разбор и цена вариантов в `docs/BACKLOG.md`. **Заказы без оплаты и рабочий стол** (тикеты 07-08): `/admin/pending` — раздел «Ждут оплаты» (`ready_for_payment` + `pending_payment`, старые сверху) и кнопка «напомнить» — она отправляет ссылку СУЩЕСТВУЮЩЕГО живого счёта и НИЧЕГО не создаёт, не продлевает и не двигает статус (иначе панель — второй способ выпускать денежные документы мимо гейтов `payments/create`); суточное окно занимается АТОМАРНО (`claimPaymentReminder`, лок строки заказа) ДО отправки — иначе две вкладки шлют клиенту два платёжных документа; сорванная доставка окно съедает, поэтому пишется отдельным событием `payment_reminder_failed` и показывается на экране; сумма в напоминании называется С надбавкой платёжной системы того шлюза, что выставил счёт; живость счёта проверяется ФАКТОМ срока, а не статусом заказа (`expire-payments` бежит раз в 15 минут — окно «статус оплатимый, ссылка мертва» реально); ссылка достаётся одним местом — выражением по снимку инвойса в репозитории. `/admin` — рабочий стол «что требует внимания» (четыре блока: ждут оплаты, проверка платежей, новые обращения, карточный счёт), без единого графика: «как идут дела» отвечает раздел «Аналитика» (с 2026-09-02) и Metabase, а пустой стол честно пишет «всё спокойно». **Поддержка и уведомления** (тикеты 10-11): `/admin/support` — обращения (единица — РАЗГОВОР, а не сообщение: «кто ведёт» и «подключиться» живут на `conversations`), лента с ролями и ответ клиенту ОТ ИМЕНИ БОТА через клиентского бота; текст оператора маскируется от номеров карт ДО отправки и записывается ПОСЛЕ неё (строка в переписке — запись произошедшего); захват диалога атомарный и чужой не перебивает. **Партнёры** (тикет 12): `/admin/partners` (привёл / начислено / баланс / ставка — ТОЛЬКО из `locked_rate_l1_bps`), раскрытие приглашённых и `/admin/partners/payouts` с двумя решениями по заявке. **Список партнёров растёт от НАЧИСЛЕНИЙ** (`referral_accruals ∪ referral_partners`), а не от таблицы профилей: строку профиля создаёт только месячный роллап 1-го числа, и заработавший 5 августа партнёр до 1 сентября отсутствовал бы на экране, который честно писал бы «Партнёров пока нет» при живых деньгах в ledger; без профиля ставка показывается дефолтная — ровно та, по которой начисление и посчитано. «Выплачено» — ДВА перехода `requested → processing → paid` через существующий атомарный `transitionReferralPayout` (прямой прыжок машина не разрешает, и `processing` страхует от повторного нажатия), «отклонить» — один переход, и компенсирующая строка в ledger НЕ пишется: формула баланса просто не считает отклонённые заявки. Операция читает ФАКТИЧЕСКИЙ статус заявки (`findReferralPayoutForPanel`) и от него пляшет: два перехода вне одной транзакции означают, что упавший между ними процесс оставляет заявку в `processing` — её сумма продолжает вычитаться из баланса, и зашей роут `from: 'requested'` намертво, вынуть такую заявку можно было бы только SQL'ем на проде (`isPayoutDecidable` поэтому пускает и `processing`; несостоявшийся переход возвращает статус ИЗ БАЗЫ, а не запрошенный — соврать о состоянии денег нельзя). **Заблокированному антифродом партнёру выплата не проводится** (`409 partner_suspended`; кабинет не даёт ему подать заявку, но поданная ДО блокировки живёт в `requested` — без гейта блокировка снималась бы одним кликом), отклонить его заявку можно: это способ её закрыть. По умолчанию экран показывает только открытые заявки (`?all=1` — все): закрытые копятся навсегда и вытеснили бы живые за потолок выборки. Панель фиксирует ФАКТ, деньги уходят вручную (`settlePayout` — mock). Реквизиты выплаты наружу не отдаются. Раздел только у владельца. ⚠️ Кто именно провёл выплату, видно ТОЛЬКО в логе — колонки актора у `referral_payouts` нет (разбор в `docs/BACKLOG.md`). Уведомления персоналу — `lib/alerts/notify-staff.ts` через бота ВХОДА, получатели из `staff` узкой выборкой без `totp_secret` и ТОЛЬКО с правом на соответствующий раздел (`capability`), дедуп по ключу с окном на вызов, 403 у одного не глушит остальных, а незаданный токен бота БРОСАЕТ (иначе «не отправлено» неотличимо от «отправлено» и обращение теряется при зелёном ответе клиенту); события: новое обращение, застрявший заказ (только если автоповтор `issue-card` НЕ помог), холд банка сразу, низкий баланс карт. ⚠️ Алёрт баланса вернулся с ДВУМЯ порогами: «не хватает на типовой заказ» (авария, раз в час) и «не хватает на самый дорогой заказ каталога» (предупреждение, раз в СУТКИ — окно задаётся на вызов, ключ с датой сам по себе суток не держит) — один порог от максимума был бы вечно красным, а такие алёрты перестают читать; выключение теперь ЯВНЫМ флагом `VCC_BALANCE_ALERT_DISABLED`, ноль в пороге означает «считать относительно», а явно заданный порог ОТМЕНЯЕТ второй уровень. Порог экрана панели считается ТОЙ ЖЕ функцией, что и крон-алёрт. Дедуп фиксируется только по факту доставки, Sentry живёт под тем же окном, а если получателей нет вовсе — уведомление уходит владельцу (`notifyOps`): на проде `staff` пуст до ручного заведения, и молчание было бы неотличимо от тишины. **Редизайн 2026-08-27 (PR #173, спека `.scratch/admin-panel-redesign/`):** все тексты панели — в ОДНОМ словаре `lib/panel/labels.ts` (термины «Ждут оплаты», «Проверка платежей», «Истёк срок», «Ошибка»; тон безличный; канарейка в `labels.test.ts` роняет тест на «ты»-формах и слове Sentry — в словаре, в `roles.ts` и в исходниках страниц; ключи адресов `?s=unpaid`/`?sort=` не менялись — пересланные ссылки живы; клиентские словари кабинета/бота словарь панели НЕ импортируют, аудитория разная). Счётчики работы в меню («Ждут оплаты», «Поддержка») — `lib/panel/menu-counts.ts`: один читатель для стола и меню, памятка 30 с держит сам запрос (незавершённый переиспользуется независимо от срока, неудача не хранится, дедлайн 2 с на вызов, неожиданный отказ — в Sentry раз в 10 мин на секцию), ответ клиенту сбрасывает её (`invalidateMenuCounts`); бейджи рендерятся в `<Suspense>`, оболочка не ждёт БД после данных страницы. Заголовок вкладки — шаблоном `%s — Панель Оплатишки` в `app/admin/layout.tsx`. **Панель v2 (2026-09-02, спека `.scratch/admin-panel-v2/`, тикеты 01–14; владелец 2026-09-01 осознанно пересмотрел «графиков в панели нет» и «настроек в базе не делаем»):** (A) **`/admin/analytics`** (только `admin`) — деньги/воронка/продукт за `?period=7|30|90` (Zod, мусор → 30; окно — календарные дни UTC, полуоткрытое `[since, until)`), выборки ТОЛЬКО в `packages/db/src/repositories/analytics-panel.ts` (выручка — `payments` `succeeded` по `completed_at`, оплаченные заказы — `orders` в `PURCHASED_ORDER_STATUSES` по `paid_at`: источники разные намеренно, они расходятся на оплаченном, но провалившемся заказе; воронка — семь шагов `ANALYTICS_FUNNEL` по вьюхе `analytics_timeline` с фильтром периода, конверсия к нулевому шагу — `null`; клики каталога по `props.slug`), деньги integer до рендера, графики — свои SVG в `components/panel/charts/` без клиентского JS и зависимостей, живое обновление выключено. (B) **`/admin/ai` — AI-аналитик** (только `admin`; ADR 0003): DeepSeek через клиент помощника поддержки (`PANEL_AI_MODEL` → `supportModel()`), один инструмент `run_sql`. **Инварианты:** защита — ГРАНТЫ и READ ONLY, а не промпт: отдельная роль `panel_ai_ro` (`packages/db/scripts/panel-ai-role.sql`: набор `metabase_ro` + таблицы воронки + вьюхи аналитики, `BYPASSRLS`, `CONNECTION LIMIT 2`; УЖЕ metabase_ro — `staff` без `totp_*`/email/telegram_id, `vpn_subscriptions` без `subscription_url`; `ALTER DEFAULT PRIVILEGES` не ставится: новая таблица = явный грант ОБЕИМ ролям + описание в словаре схемы, зеркало ловит тест `schema-dictionary.test.ts`); исполнитель `runReadOnlyQuery` (`packages/db/src/readonly-query.ts`) — своё подключение по `PANEL_AI_DATABASE_URL`, `getDb()` не перенацеливается, транзакция `READ ONLY` + `SET LOCAL statement_timeout`, запрос завёрнут в подзапрос с `LIMIT`, потолки 200 строк / 32 КБ, ошибка SQL — Result с текстом Postgres (модели он нужен, чтобы поправиться), никогда не бросает. ⚠️ **«Одно выражение» держит СЕРВЕР, а не наш разбор SQL:** запрос уходит extended protocol (`tx.unsafe(sql, [], { simple: false })`), и строку из нескольких команд Postgres отвергает сам. `unsafe()` без параметров идёт simple protocol и исполняет `;`-цепочку целиком — вместе с `COMMIT; BEGIN READ WRITE`, снимающим read-only транзакцию, а ручной лексер `validateReadOnlySql` расходился с Postgres на `$` внутри идентификатора и на `E'\''`, так что единственным барьером оставались гранты (code-review 2026-09-02). Валидация инструмента (`SELECT`/`WITH`, без `INTO`/`FOR UPDATE`/`pg_sleep*`/`COPY`/large objects) осталась как понятный отказ модели; провайдер видит только то, что отдаёт роль, строковые ячейки И текст ошибки Postgres — через `maskForModel` (ошибка несёт значение ячейки), а uuid и цифровые идентификаторы НЕ маскируются (аналитик обязан указать на клиента); чат ЭФЕМЕРНЫЙ — история в клиенте (Zod ≤ 20 ходов / 8 КБ), в БД ничего, в `ai_usage_daily` НЕ пишется (учёт — строка лога `panel.ai.usage`), кап `panel-ai` 30/10 мин по `staff.id` fail-open; операция под гейтом `Origin`+JSON (вопрос стоит денег); без ключа или URL роли — «не настроено», не 500. Eval с живым ключом — `pnpm --filter web eval:panel-ai` (не в CI). (C) **`/admin/texts` — тексты воронки** (только `admin`): миграции 0043 (`funnel_texts` оверлей, `funnel_text_revisions` история) и 0044 (append-only триггер истории; у истории FK на `staff` НЕТ намеренно — `ON DELETE SET NULL` это UPDATE, который триггер отверг бы, и удаление сотрудника падало бы на его правках); реестр 22 ключей — `lib/funnel/texts.ts` (см. пункт воронки), одна `validateFunnelText` на сохранение и тест-отправку (`empty`/`missing_placeholder`/`unknown_placeholder`/`too_long`/`duplicate_label`), сохранение и сброс пишут историю в той же транзакции и инвалидируют памятку; тест-отправка — КЛИЕНТСКИМ ботом на `staff.telegram_id` БЕЗ клавиатуры (кнопки `fb:*` от сотрудника писали бы `client_feedback` ему как клиенту), `{service}`=Netflix, `{link}`=`ref_TEST` тем же билдером, что в кабинете, в `funnel_sends`/`client_feedback`/`messages` ничего не пишется, кап `panel-texts-test` 10/мин. (D) **Счётчик «Проверка платежей»** в меню — `countHoldsForPanel` с ОБЩИМ `holdsCondition()` для списка и счётчика (зеркало убрано), считает заказы distinct без потолка, ручная выдача инвалидирует слот `holds`; **`/admin/feedback` — «Обратная связь»** (`admin` + `operator`): сводка отправлено/ответов/доля по видам за период и лента ответов (оценки 1–3 — `danger`, ссылки на клиента и заказ), read-side в `repositories/funnel.ts`, счётчик в меню — ответы за 24 ч, состояния «просмотрено» нет намеренно. ⚠️ Прод-шаги трека (роль `panel_ai_ro`, env `PANEL_AI_DATABASE_URL`, миграции 0043/0044) — руками владельца, см. `.scratch/admin-panel-v2/issues/15` и CHANGELOG.
- **Тесты**: Vitest в `apps/web`, `packages/types`, `packages/db` (**интеграционные на PGlite** — реальный Postgres + реальные миграции; иначе атомарные claim'ы, append-only-триггер и RLS не проверить) и `packages/agent`. Всего **web 2046, types 189, db 372, agent 69** (2026-09-02, трек admin-panel-v2 после code-review). ⚠️ **Пакет без `test`-скрипта молча пропускается** прогоном `pnpm -r --if-present test` — так `packages/agent` числился покрытым и не гонялся до 2026-08-11; при добавлении пакета проверять, что скрипт есть. `pnpm typecheck` проверяет и тесты (L-14). Перечень покрытия и какой инцидент породил каждый регресс — [`docs/reference/testing.md`](docs/reference/testing.md).

## Фаза 2 — виртуальные карты (PaySpace)

Карты выпускает **app.pay.space** (это НЕ Love&Pay: L&P и Freekassa принимают рубли, PaySpace
выпускает карту, которой клиент платит на сайте сервиса). Контракт подтверждён живыми вызовами.

**Инварианты, которые нельзя нарушать:**

- **`createCard` НЕ ретраится** (`idempotent:false`) — единственная мутирующая операция без
  идемпотентного ключа у провайдера: повтор на таймаут/5xx выпустил бы вторую профинансированную
  карту-призрак. Остальные операции идемпотентны через `request_id` и ретраятся ×2.
- **Атомарный claim `paid → in_fulfillment` ДО операций** (`transitionOrderDetailed`) — at-most-once
  выпуск; recovery потерянных — cron `poll-payment`.
- **Полные `pan`/`cvc` не пишутся в логи, БД и Sentry** — только `pan_masked`. Санкционированных
  каналов выдачи ровно два: сообщение в Telegram при выпуске и разовый показ в кабинете. Других
  добавлять нельзя без threat-model.
- **Дока провайдера врёт** — суммы приходят то строкой, то числом, `exp_date` в формате `MM/YY`,
  опциональные поля бывают `null`. Всё через Zod, дрейф → `PaySpaceContractError`.
- **Операционный гейт — баланс VCC-субаккаунта:** на каждую карту нужно `цена + буфер (+$4 fee)`.
  Сколько именно снимется с фонда ради заказа, знает ОДНА функция
  `orderFundingRequirementUsdCents` (`lib/pay-space/funding.ts`) — её же зовут порог алёрта
  низкого баланса и preflight; правило «пригодна ли живая карта для долива» тоже одно
  (`isCardReusable`, оно же в `issue-card`).
- **Preflight фонда перед выставлением счёта** (на проде с 2026-08-25, PR #171): `POST /api/payments/create`
  последним шагом перед созданием счёта проверяет, есть ли чем выпустить карту, и при нехватке
  отдаёт `422 fulfillment_capacity` — деньги не принимаются вовсе. Гейт стоит на ВЫСТАВЛЕНИИ
  счёта, а не на приёме денег (`repeat_confirm` проходит мимо); отказ НЕ трогает заказ
  (`ready_for_payment` с зафиксированной ценой — перевод в `expired`/`failed` был бы наказанием
  клиента за нашу проблему); **fail-open** — непрочитанный баланс оплату пропускает, выключатель
  явным флагом `PAYSPACE_PREFLIGHT_DISABLED`. Баланс берётся из СВОЕГО снимка
  `vcc_balance_snapshots` (его пишет крон опроса баланса, свежесть 30 мин; протух — один живой
  запрос с поводком 2 с), поэтому в горячем пути провайдер не нужен и его недоступность продажи
  не останавливает. Свободным считается **остаток минус уже обещанные карты**
  (`findOrdersCommittingCardFund`: `pending_payment` с живым счётом, `paid`, `in_fulfillment`)
  минус `PAYSPACE_SAFETY_RESERVE_USD_CENTS`; ⚠️ `ready_for_payment` в обязательства НЕ входит —
  иначе заказ вычитал бы сам себя. ⚠️ `VCC_BALANCE_ALERT_DISABLED` гасит СООБЩЕНИЯ, а не опрос — иначе флаг
  «не шуметь» ломал бы питание снимка. **Проверка и занятие фонда — одна неделимая операция** под
  `pg_advisory_xact_lock('vcc-funding')` (тикет 05): заказ становится `pending_payment` только
  ПОСЛЕ ответа шлюза, и в этом окне двое видели одни и те же деньги. Замок держится на время
  РАСЧЁТА, а не вызова шлюза; резерв (`vcc_fund_reservations`, одна строка на заказ) живёт до
  срока счёта и учитывается, только пока заказ в `ready_for_payment`. ⚠️ Не удалось занять (таймаут замка) — ОТКАЗ, а не пропуск:
  fail-open выключал бы защиту ровно под нагрузкой. Остаточный риск просадки между гейтом и
  выпуском сохраняется. Тексты, дедупы, событие журнала и разбор —
  [`docs/reference/virtual-cards.md`](docs/reference/virtual-cards.md).
- Срок жизни карты — одно число `CARD_LIFETIME_DAYS`, зеркало с витриной кабинета (инвариант 10).

Клиент, буфер под VAT/FX, модель префандинга, классификация отказов топапа и рециклинг —
[`docs/reference/virtual-cards.md`](docs/reference/virtual-cards.md).

## Структура

Раскладка каталогов и зона ответственности каждого пакета — `docs/architecture.md`,
раздел «Файловая система: кто за что отвечает». Список таблиц с колонками и ограничениями —
`packages/db/src/schema.ts` (источник правды, 21 `pgTable`); RLS включён на всех.
Плюс последовательность `freekassa_nonce` (не таблица) — монотонный nonce запросов к Freekassa.

## Границы пакетов (строго!)

| Пакет | Может импортировать | Запрещено |
|---|---|---|
| `@oplati/types` | только `zod` | `@oplati/*` |
| `@oplati/db` | `@oplati/types` | `@oplati/agent`, `apps/web` |
| `@oplati/agent` | `@oplati/types` | **`@oplati/db` напрямую** (через `ToolHandlers`) |
| `apps/web` | все `@oplati/*` | — |

`@oplati/agent` общается с БД только через интерфейс `ToolHandlers` (реализация в `apps/web/lib/tool-handlers/`). Импорты — только через barrel или объявленные subpath-exports (`@oplati/db`, `@oplati/db/schema`, `@oplati/agent/tools`); приватные пути (`@oplati/db/src/...`) и `../../../` cross-package imports запрещены.

## Архитектурные инварианты (не нарушать)

1. **`order_events` — append-only.** Никогда не `UPDATE`/`DELETE`. Любое изменение статуса = новая строка в той же транзакции, что меняет `orders.status`. Форсится триггером БД `order_events_append_only` (миграция 0018) — `UPDATE`/`DELETE` бросают exception даже для `service_role` (RLS его не покрывает).
2. **Идемпотентность webhook'ов** — `UNIQUE(provider, provider_ref)` на `payments` + `INSERT ... ON CONFLICT DO NOTHING`. Повторный вызов не создаёт дубль или двойной переход. Anti-replay webhook'а L&P держится на атомарном `claimPaymentSucceeded` (подпись без timestamp/nonce), а claim платежа + `transitionOrder(paid)` идут в ОДНОЙ транзакции (`processInvoicePaid`) — сбой перехода откатывает claim, иначе оплаченный заказ застревал бы без recovery. Плюс частичный `UNIQUE(order_id) WHERE status='pending'` — не более одного живого инвойса на заказ. Терминальные события L&P (`expired`/`cancelled`) переводят платёж через симметричный атомарный `claimPaymentTerminal` (`pending→failed` условным UPDATE, null-возврат = idempotent_skip) — НЕ безусловный UPDATE: при гонке с paid-путём не перезаписывает уже `succeeded` платёж в `failed`.
3. **Деньги — integer в минимальных единицах.** `amount_rub` — копейки, `original_amount` и `balance_usd_cents` — USD-центы. Никогда `numeric`/`float`.
4. **State-переходы заказа — только через `transitionOrder()`** (`packages/db/src/repositories/orders.ts`). Прямой `UPDATE orders SET status` запрещён. Разрешённые переходы — `allowedTransitions` в `packages/types/src/order-state-machine.ts`.
5. **Zod на всех границах.** Webhook body, Telegram updates, AI tool inputs, URL params — парсятся схемой из `@oplati/types`. Не `any`, не `as T` без обоснования.
6. **Webhook endpoints всегда `200 OK`** (даже при невалидном input — ошибка в теле), иначе Telegram/L&P ретраят и забивают очередь. Исключение — неверный `X-Telegram-Bot-Api-Secret-Token`: `401` отдают `/api/bot` (клиентский бот) и `/api/staff-bot` (бот персонала, свой секрет).
7. **Каталог `services` — public read, не public write.** RLS включён; `anon/authenticated` имеют только `SELECT` по policy `services_public_read_active` (`is_active=true`). Seed/изменения каталога — через server-side/service role и Drizzle.
8. **Весь доступ к user-таблицам — только через `service_role`/прямое подключение.** RLS на них — deny-by-default без позитивных политик (браузерный anon-клиент не читает ничего, кроме активного каталога). Модель безопасна, пока не появится клиентский Supabase-запрос к user-данным: тогда deny-by-default его заблокирует — понадобится per-user policy, а не ослабление RLS.
9. **Неаутентифицированные write-эндпоинты — под rate-limit.** `/api/chat`, `/api/orders/propose`, `/api/orders/confirm`, `/api/auth/telegram/link`, `/api/chat/clear`, `/api/cabinet/referral` (веб-ветка) зовут `checkRateLimit` по IP ДО резолва сессии и записей в БД: без cookie каждый запрос иначе получает свежую сессию и свежий суточный кап (cost-DoS на строки users/orders/link_tokens). `POST /api/cabinet` авторизован подписью `initData`, поэтому сплошного IP-лимита у него НЕТ намеренно (за CGNAT и за собственным VPN Оплатишки один адрес на всех клиентов — общий бакет резал бы живых плательщиков за чужой флуд): по IP считаются ТОЛЬКО похожие на подделку отказы подписи и битые тела (`cabinet-auth`, приём как у `alert-webhook-auth`; `expired` и `misconfigured` в счёт НЕ идут — это штатное протухание сессии и наша же авария конфига), а опознанный запрос лимитируется по `telegram_id` в СВОЁМ бакете `cabinet` (не общем с ботом) и обязательно ДО upsert'а `users` — для этого `resolveCabinetUser` разделён на `verifyCabinetInitData` (чистая проверка подписи) и `upsertCabinetUser` (запись). ⚠️ Соседний `/api/cabinet/referral` на эту схему ещё НЕ переведён (см. `docs/BACKLOG.md`). В боте три бакета по той же причине «свой кошелёк на каждый вход»: `/start` — `telegram-start` (это шаг оплаты, `link_<token>`; ронять его чужим трафиком нельзя), медиа — `telegram-media` (Telegram шлёт по апдейту на каждое фото альбома), текст и callback-кнопки — `telegram`. `GET /api/orders/status` — read-only (`readWebSessionId` + `findUserIdByWebSessionId`, пользователя НЕ создаёт) + ОТДЕЛЬНЫЙ бакет `web-order-status` (60/мин, аудит 2026-07-28): после оплаты клиент опрашивает статус каждые 4 с (≈15/мин), и общий с `propose`/`confirm` бакет на 8/мин он выедал сам — часть поллов ловила 429, а создание следующего заказа с того же IP блокировалось на 5 минут (за CGNAT мобильных операторов это несколько живых клиентов в одном IP). **Источник identity зависит от `CLIENT_IP_MODE` (дефолт `traefik` с 2026-07-28).** На проде он же: доверенный источник — **ПРАВЫЙ элемент `x-forwarded-for`** (его пишет сам Traefik из адреса соединения), а клиентский `x-real-ip` игнорируется — за Traefik он проходит насквозь и подделывается. Значение нормализуется и валидируется как IP (`node:net`.isIP): без этого прокси с `host:port` давал бы новую identity на каждом соединении (эфемерный порт) и лимит обходился бы полностью; мусор ключом не становится, невалидный правый элемент → `unknown` (fail-closed, БЕЗ добора левее). В режиме `vercel` (исторический; ДЕФОЛТОМ быть перестал — прежний дефолт при потере переменной молча включал доверие к подделываемому за Traefik `x-real-ip`, то есть полный обход лимита, CWE-348) берётся `x-real-ip`, а НЕ левый `x-forwarded-for` (подделываем клиентом → ротация обнуляла лимит, CWE-348); `xff` — только fallback (правый элемент). Третья ветка — `X-Client-IP` при timing-safe совпадении `PROXY_SHARED_SECRET` — осталась от реверс-прокси РФ-доступа эпохи Vercel ([`docs/history/vercel-era.md`](docs/history/vercel-era.md)); на проде секрет НЕ задан, поэтому ветка мертва. Оживлять её можно только вместе с прокси: без него заголовок подделает любой клиент (тот же CWE-348). Fail-open при незаданном Upstash.

10. **Зеркало — два значения, обязанные совпадать, без автосверки.** Правишь одну сторону — той же правкой правишь вторую; ничто не упадёт, разъедется только поведение. Известные зеркала помечены словом «зеркало» на месте. Сейчас их четыре: потолок **$1200** (`HIGH_VALUE_SERVICE_SLUGS` ↔ `lib/telegram/amount.ts` ↔ `StartScreen.tsx` ↔ `CatalogView.tsx` ↔ `MAX_ORDER_USD_CENTS` в `lib/jobs/vcc-balance.ts`), `FREEKASSA_BUYER_FEE_PERCENT` ↔ ползунок комиссии в кабинете Freekassa (API комиссий у них нет — разъезд означает обещание клиенту неверной цены), `CARD_LIFETIME_DAYS` ↔ срок в витрине кабинета и **свежесть снимка фонда** `SNAPSHOT_FRESH_MS` (30 мин, `lib/pay-space/preflight.ts`) ↔ шаг крона `poll-payment` в `infra/crontab.example` (5 мин): переведёшь крон на час — каждая оплата снова пойдёт к провайдеру живьём. Зеркало — плата за отсутствие общего источника, поэтому предпочтительный ход — не «сверить внимательнее», а **убрать зеркало**: так сделано с ценой (обе точки расчёта зовут одну функцию) и с журналом миграций (расхождение ловит `GET /api/ready` и красит деплой).

## Команды

Обычные скрипты (`dev`, `build`, `typecheck`, `lint`, `test`) — в `package.json` корня и
пакетов, запуск через `pnpm` / `pnpm --filter <pkg>`. Здесь только то, чего в скриптах не
прочитать:

```bash
pnpm --filter @oplati/db db:generate    # сгенерировать миграцию из schema.ts
pnpm --filter @oplati/db db:push        # применить схему напрямую (ТОЛЬКО dev; на проде — db:migrate)
pnpm --filter @oplati/db db:migrate     # применить через migrate (.env из корня)
pnpm --filter @oplati/db db:seed        # seed каталога сервисов (⚠️ прод-каталог живёт в БД, мерж его не обновляет)
pnpm --filter @oplati/db db:staff list|add|disable|enable|reset-totp   # персонал панели (секрет TOTP не выдаёт — его создаёт панель при первом входе)
pnpm --filter @oplati/db db:init-roles  # роли БД (read-only `metabase_ro`, см. runbooks/metabase.md)
```

⚠️ Все `db:*`-скрипты грузят `.env` из корня, а он указывает на старые Supabase-базы: на проде
персонал заводится тем же `INSERT … ON CONFLICT`, что в `upsertStaffByTelegramId`, через `psql`
в контейнере `oplatishka-db-ry3smb` (порядок ssh-команд — скилл `deploy-and-migrations`).

### Миграции БД

**Forward-only через Drizzle.** Схема — `packages/db/src/schema.ts`. Правка схемы → `db:generate` → `.sql` в `packages/db/migrations/` → `db:push` (или `db:migrate`, если push не видит `DATABASE_URL`). Никогда не редактировать применённую миграцию и не править БД через Supabase Dashboard в обход Drizzle. Destructive-изменения — только backwards-compatible (nullable-колонки, два деплоя на удаление).

⚠️ **Миграция, УДАЛЯЮЩАЯ колонку, применяется ПОСЛЕ выката кода, а не до.** Drizzle на `db.select().from(t)` генерирует ЯВНЫЙ список колонок из `schema.ts` РАБОТАЮЩЕГО образа — значит образ, выпущенный до удаления, продолжает спрашивать колонку и получает `column ... does not exist` на каждом запросе. Grep «поле нигде не читается» это не ловит: он гоняется по новому коду, а ломается старый. В окне между выкатом и миграцией `/api/ready` законно красный (`migrations_pending`), но приложение ЖИВО — лишняя колонка безвредна. Обратная сторона: откат на образ старше такой миграции кладёт то, что читает эту таблицу (порядок и способ вернуть колонку — [`docs/runbooks/rollback.md`](docs/runbooks/rollback.md)).

⚠️ **Расширение enum — ПЕРЕСОЗДАНИЕМ типа, а не `ALTER TYPE ADD VALUE`.** Postgres запрещает использовать значение enum в той же транзакции, где оно добавлено к СУЩЕСТВУЮЩЕМУ типу. Разнести добавление и использование по двум файлам НЕ помогает: drizzle-мигратор оборачивает в ОДНУ транзакцию ВСЕ ожидающие миграции разом (`PgDialect.migrate` — `session.transaction()` вокруг цикла по файлам), а не каждый файл по отдельности. Пара «файл с `ADD VALUE` + файл, который его использует» падает с `unsafe use of new value`, как только оба ждут применения — то есть при обычном инкрементальном обновлении dev-базы. Коварство в том, что прогон С НУЛЯ зелёный: там сам тип создаётся в этой же транзакции, и запрет не действует. Рабочая форма — rename + `CREATE TYPE` + swap колонки + `DROP TYPE` старого в одной миграции (на тип, созданный в текущей транзакции, запрет не распространяется); образец — `0041_conscious_lilith.sql`, ловушка и лекарство закреплены тестом `packages/db/src/migrator.integration.test.ts`. Ручное применение через `psql` по файлу (автокоммит) от этого не страдало — потому баг и не всплывал.

⚠️ **ДЕПЛОЙ НЕ ПРИМЕНЯЕТ МИГРАЦИИ. После каждого мержа с новой миграцией применить её на прод-БД отдельно — сразу, а не «когда включим фичу».** Пайплайн собирает образ и перезапускает сервис, не более. 2026-07-28 этот шаг потерялся: код Freekassa уехал в `main` 26.07, миграции 0025/0026 на прод не попали, и при включении шлюза первый же счёт упал с `relation "freekassa_nonce" does not exist` — **при зелёном деплое, здоровом `/api/health` и без единого алёрта** (см. [`docs/incidents.md`](docs/incidents.md)).

С 2026-07-29 такая схема больше не молчит: **`GET /api/ready`** сравнивает журнал миграций,
запечённый в образ, с журналом в живой БД (и свежесть, и ЧИСЛО применённых), и шаг «Проверить
готовность релиза» в `deploy.yml` красит деплой. Liveness `/api/health` БД по-прежнему не
трогает. Обнаружение автоматизировано, применение — руками: прод-БД снаружи недоступна,
порядок ssh-команд и дописывания журнала — скилл `deploy-and-migrations`, пошагово —
[`docs/runbooks/deploy.md`](docs/runbooks/deploy.md).

## Deployments

**Весь контур — на одном VPS `187.124.172.104`** (Hostinger, дата-центр Франкфурт, 2 vCPU / 8 ГБ),
Dokploy + Traefik + Docker. Машина отдана только Оплатишке. ⚠️ Единственное исключение —
**VPN-панель Remnawave `panel.mxpkn8ns.ru` на бостонском VPS `177.7.34.106`**, туда ходит кнопка
«VPN» в боте.

| | Production | Dev |
|---|---|---|
| Приложение Dokploy | `oplatishka-web` | `oplatishka-web-dev` |
| Ветка | `main` | `dev` |
| Домены | `www.oplatishka.com` + apex, `new.oplatishka.com` (на нём бот-webhook) | `dev.oplatishka.com` (за Basic Auth) |
| БД | self-host Postgres 17 `oplatishka-db` | `oplatishka-db-dev` (структура = prod, без клиентских данных) |
| Бот | `@oplatishkaa_bot` | `@dev_test_podpiska_bot` |
| Модель | `claude-sonnet-4-6` | Haiku (дешевле) |
| Rate-limit | self-host Redis + SRH | `RATE_LIMIT_DISABLED=1` |
| L&P / PaySpace | боевые ключи | **ключей НЕТ намеренно** — иначе тестовый заказ выставил бы реальный счёт и выпустил карту за реальные деньги |
| Sentry | `SENTRY_DSN` + `NEXT_PUBLIC_SENTRY_DSN` в **Build Args** | не задаём: проект в Sentry один, alert-rules общие |

**Деплой — ТОЛЬКО через `.github/workflows/deploy.yml`** (push в `main`/`dev` → гейт
typecheck+тесты+lint → deploy-вебхук Dokploy → **проверка, что прод реально обновился** по
`startedAt` из `/api/health`). Принятый триггер не равен выкаченному релизу — пока проверки не
было, упавшая сборка давала зелёный workflow при старом коде на проде. Прямой push в `main`
запрещён ruleset'ом, merge только squash.

⚠️ **Деплой НЕ применяет миграции** — см. «Миграции БД» выше. `GET /api/ready` сравнивает журнал
миграций в образе с журналом в живой БД и красит деплой, если код уехал без них.

⚠️ **Каталог сервисов живёт в БД, а не в коде** — мерж в `main` не добавит сервис в витрину,
нужен отдельный seed на прод-БД (витрина кэшируется в памяти инстанса 5 минут).

⚠️ **dev-домен под Basic Auth, а Telegram его не умеет** — WebView Mini App и серверы Telegram
получают `401`. Исключения вынесены отдельными файлами Traefik на VPS; при добавлении экранов в
Mini App сверять список путей (`grep -rhoE "'/api/[a-z/_-]*'" components/cabinet`).

Устройство VPS, панель Dokploy, hardening, контракт deploy-вебхука, работа с dev-БД и
автовыравнивание `dev` — [`docs/reference/infrastructure.md`](docs/reference/infrastructure.md);
процедуры — [`docs/runbooks/deploy.md`](docs/runbooks/deploy.md).

### Telegram-секреты

> Реальные токены — только в env приложения Dokploy и локальном `.env.local`/`.env`
> (gitignored). Никогда не пастовать в файлы, коммиты, чат. Компрометация: `/revoke` у
> `@BotFather`, `openssl rand -hex 32` для нового webhook-secret. **После смены секрета
> обязателен редеплой** — контейнер держит значение с момента старта.

**Webhook у бота один** — dev-бот и прод-бот потому и разные. Где какой env лежит, как его
править и как переставить webhook без раскрытия токена — скилл `deploy-and-migrations`;
полный список переменных — [`docs/reference/env-vars.md`](docs/reference/env-vars.md).

## Конвенции кода

- **`strict: true`** + `noUncheckedIndexedAccess` + `verbatimModuleSyntax`. `any` запрещён; `unknown` + Zod narrow.
- **Never swallow errors** — `catch {}` и `catch { console.log }` запрещены: либо re-throw, либо `Sentry.captureException` + structured error.
- **Result pattern** для ожидаемых неудач (`{ ok: false, reason }`); `throw` — для неожиданного.
- **`console.log` запрещён** в production-коде — только `logger.*` (pino). PAN/CVC/токены не логируются никогда.
- **`fetch` без timeout запрещён** — всегда `AbortController`, и таймаут обязан покрывать ЧТЕНИЕ ТЕЛА, а не только заголовки (аудит 2026-08-10): сервер, отдавший `200` и замолчавший на теле, вешал запрос навсегда. Клиенты PaySpace/L&P перевзводят таймер на чтение тела своим бюджетом (у `createCard`, `POST /invoices`, `revoke` ретрая нет — оборванное тело значит осиротевшую операцию у провайдера). Серверный код читает JSON через `fetchJsonWithTimeout` из `lib/http.ts`; `fetchWithTimeout` снимает таймер до чтения тела и годится только браузеру. Транспорт undici — это `TypeError('fetch failed')` И `TypeError('terminated')` (обрыв сокета уже ПОСЛЕ заголовков), оба ловит `isNetworkTypeError`.
- **`Date` в raw-`sql`-фрагменты Drizzle передавать нельзя — только ISO-строку** (`cutoff.toISOString()`, паттерн `analytics.ts`): postgres-js на проде падает `TypeError … Received an instance of Date` при сериализации параметра, а PGlite в тестах `Date` переваривает — регресс невидим в зелёном прогоне (инцидент 2026-08-15, PR #162). Параметры обычных builder-условий (`eq`, `lt`, `gte`, …) не затронуты — там drizzle маппит `Date` сам.
- **Commits — Conventional Commits** (`feat(agent):`, `fix(payments):`); squash merge; заголовок ≤ 72 символа.
- Graceful degradation на внешних зависимостях (Anthropic, БД): понятный ответ пользователю, не 500.

## MCP-серверы

`.mcp.json`: `github`, `filesystem`, `chromeDevtools`, `playwright`.

**Dokploy MCP вернулся 2026-07-28** — но НЕ в `.mcp.json`, а в **local scope**
(`~/.claude.json`, мимо git: репозиторий публичный, а нужны API-ключ и пароль
basic-auth). Возражение, из-за которого его убирали 27.07 («умеет только
`x-api-key`, значит пришлось бы открыть `/api` наружу»), снято переменной
`DOKPLOY_CUSTOM_HEADERS`: в ней едет `Authorization: Basic …`, поэтому проходятся
ОБА барьера, а `/api` остаётся закрытым. Проверено живым вызовом:
`x-api-key` без basic-auth снаружи получает `401` от Traefik.

Управление контуром без MCP (и как fallback) — ssh + API с самого VPS:
```bash
ssh root@187.124.172.104 'curl -s -H "x-api-key: <ключ>" http://127.0.0.1:3000/api/project.all'
```
На `127.0.0.1:3000` запрос идёт мимо Traefik, поэтому basic-auth не участвует.

⚠️ **Правка env через API перезаписывает его ЦЕЛИКОМ**, `saveEnvironment` требует
ещё `buildArgs`/`buildSecrets`/`createEnvFile` (иначе `400`), а в БД панели env
лежит зашифрованным. Порядок с бэкапом и построчной сверкой — в
[`docs/runbooks/deploy.md`](docs/runbooks/deploy.md).

**Supabase MCP убран 2026-07-25:** боевая БД — self-hosted Postgres на VPS, и MCP туда не ходил; оставленный, он выглядел как доступ к проду, а отдавал данные холодного резерва. Запрос к боевой БД — через ssh:
```bash
ssh root@187.124.172.104 'docker exec $(docker ps --filter name=oplatishka-db-ry3smb -q) \
  psql -U oplatishka -d oplatishka -c "select count(*) from orders"'
```
Миграции — только через Drizzle, ни через MCP, ни руками.

## Agent skills

Конфигурация инженерных скиллов (`mattpocock-skills`) — в `docs/agents/`.

### Трекер задач

Тикеты и спеки — локальным markdown в `.scratch/<feature>/` (в `.gitignore`: репозиторий
публичный). Отложенное живёт в `docs/BACKLOG.md` и в `.scratch/` не дублируется.
См. [`docs/agents/issue-tracker.md`](docs/agents/issue-tracker.md).

### Метки триажа

Пять канонических ролей, состояние строкой `Status:` в теле тикета; `ready-for-agent` на
денежных путях ставит только владелец. См. [`docs/agents/triage-labels.md`](docs/agents/triage-labels.md).

### Доменные документы

Single-context: источник домена — этот файл + `docs/architecture.md`, ADR новых решений —
`docs/adr/`, глоссарий `CONTEXT.md` создаётся лениво.
См. [`docs/agents/domain.md`](docs/agents/domain.md).

## Что запрещено

- Кросс-импорты между `apps/*`, циклы между пакетами, импорт приватных путей пакетов.
- `pnpm --filter @oplati/db db:push --force` на prod.
- Commit `.env*` / реальных токенов (`.gitignore` покрывает — не отключать).
- Использовать prod Supabase / Telegram-бот / кабинет Love&Pay для локальной разработки.
- Эмодзи в коде, комментариях, логах (в русских UI-строках — можно, если требует продукт).
- Логировать или сохранять полные PAN/CVC карт.
- Выдумывать контракт внешнего API (PaySpace, L&P, Rapira) — только подтверждённый живым вызовом или докой владельца.
