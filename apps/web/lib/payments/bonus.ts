/**
 * Отказ «списать баллы сейчас нечем» — маркер и текст клиенту.
 *
 * Живёт рядом с `capacity.ts` и по той же причине: код нужен продюсеру
 * контракта (`payments/create`), классификатору (`confirm_order`) и клиентским
 * каналам, а тянуть ради строки server-only-модуль в компонент кабинета нельзя.
 *
 * ⚠️ Почему 409, а не 422. Тело запроса корректно и условие клиент выполнил —
 * баллы у него были, когда он открывал экран. Это КОНФЛИКТ состояния: пока он
 * думал, параллельная заявка на вывод (или другой заказ) забрала те же центы.
 * 409 в проекте уже значит ровно это — `invalid_status`, `order_expired`.
 *
 * Тихо выставить полный счёт вместо отказа нельзя: клиент нажал «оплатить со
 * списанием баллов», и счёт на другую сумму был бы обманом.
 */
export const BONUS_UNAVAILABLE = 'bonus_unavailable';

/**
 * Текст клиенту. Ни цифр, ни причины: «параллельная заявка на вывод» ему
 * ничего не объясняет, а действие нужно одно — обновить экран и увидеть
 * актуальный баланс.
 */
export const BONUS_UNAVAILABLE_TEXT =
  'Баланс баллов изменился, пока ты оформлял заказ. Обнови экран — покажу, сколько можно списать сейчас.';
