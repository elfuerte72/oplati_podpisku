/**
 * Сроки хранения данных — ОДИН источник на проект.
 *
 * Отдельным модулем (без `server-only`), потому что числа нужны и крону, и
 * экранам: панель поддержки объясняет обрыв ленты словами «переписка старше
 * N дней удаляется», и это объяснение обязано быть верным. Копия числа в тексте
 * — зеркало, которое разъезжается молча и врёт ровно тому, кто его читает.
 */

/** Переписка (`messages`). Крон `retention`, 04:15. */
export const MESSAGES_RETENTION_DAYS = 90;

/** Сырые ответы провайдеров (`payments.raw_payload`). */
export const PAYLOAD_RETENTION_DAYS = 180;

/** Поведенческая аналитика (`analytics_events`). */
export const ANALYTICS_RETENTION_DAYS = 400;
