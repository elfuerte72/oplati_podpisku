/**
 * Лимиты Telegram Bot API, нужные и отправке, и шаблонам, и реестру текстов
 * воронки. Отдельный лист-модуль без зависимостей: `send.ts` тянет grammY и
 * логгер, и импорт константы оттуда протаскивал бы их в модули, которым нужен
 * только размер сообщения (а тесты, подменяющие `./send` целиком, роняли бы
 * загрузку соседних модулей).
 */

/** Максимальная длина текста сообщения. */
export const TELEGRAM_MESSAGE_LIMIT = 4096;

/** Максимальная длина подписи inline-кнопки. */
export const TELEGRAM_BUTTON_LIMIT = 64;
