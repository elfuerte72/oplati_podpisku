# Бэклог — отложенные задачи

Задачи, осознанно отложенные владельцем (не баги — прод работает без них).
Правила ведения: новая задача — с датой и контекстом «почему отложено»;
закрытая — вычёркивается здесь и попадает в [`CHANGELOG.md`](CHANGELOG.md).
История закрытых аудитов — [`audit-2026-07-18-fix-plan.md`](audit-2026-07-18-fix-plan.md)
и [`history/`](history/).

---

## Безопасность

### CSP → боевой режим (L-21 аудита; отложено 2026-07-19)

Content-Security-Policy с 2026-07-11 работает в режиме `Report-Only`
(наблюдение: браузер сообщает, что заблокировал бы, но не блокирует).
Жалоб за период наблюдения не было — можно включать enforce.

- **Что делать:** в `apps/web/next.config.ts` заменить заголовок
  `Content-Security-Policy-Report-Only` на `Content-Security-Policy`.
- **Риск:** если какой-то легитимный скрипт/стиль не учтён в политике, у части
  пользователей отвалится кусок сайта. Откат — вернуть `-Report-Only` + redeploy.
- **Перед включением:** ещё раз просмотреть CSP-репорты в Sentry за последнюю
  неделю; после включения — смоук сайта (чат, каталог, оплата, партнёрка).

### Внешний uptime-мониторинг VPS-прокси L&P (хвост H-3; отложено 2026-07-19)

Squid-прокси `177.7.34.106:24128` — единственный путь платежей к L&P (SPOF).
Внутренний healthcheck есть (cron poll-payment, 5 мин), но он живёт на том же
Vercel — нужен независимый «второй глаз». От резервного IP владелец отказался
(2026-07-19).

- **Что делать (не код, ~10 минут):** зарегистрироваться на uptimerobot.com
  (free) → Add Monitor → тип **Port**, host `177.7.34.106`, port `24128`,
  интервал 5 мин → alert-контакт: email (+ телеграм-интеграция по желанию).
- **Что даст:** письмо/пуш при падении VPS, даже если весь наш код лежит.

## Инфраструктура

### Cloudflare-прокси перед Vercel — сайт без VPN из РФ (В РАБОТЕ с 2026-07-22)

РКН/ТСПУ блокирует IP-диапазоны Vercel на части потребительских провайдеров РФ —
`oplatishka.com` у таких пользователей не открывается без VPN (DNS корректен,
из московских ДЦ-узлов check-host сайт отвечает 200; блокировка неравномерная).
Блокировка бьёт и по Mini App `/cabinet` (тот же домен). 2026-07-22 владелец
поднял приоритет: доступность сайта из РФ без VPN — принципиальное условие;
Telegram-часть не трогаем (ЦА сидит в TG через VPN). DNS домена уже на
Cloudflare (серое облако = только DNS).

- **Чек-лист включения (по порядку):**
  1. ~~Правка `getClientIp` (`apps/web/lib/ratelimit.ts`) — доверенное чтение
     `CF-Connecting-IP` через секрет Transform Rule (домен `*.vercel.app` идёт
     мимо CF — голое чтение заголовка = спуфинг CWE-348).~~ **Сделано** —
     ветка `feat/cf-proxy-ru-access`, env `CLOUDFLARE_PROXY_SECRET` (не задан →
     поведение прежнее), см. CHANGELOG 2026-07-22.
  2. CF SSL/TLS = Full (Strict).
  3. Выключить ECH (ТСПУ режет TLS с ECH — без этого CF-прокси сам под блоком).
  4. Сгенерировать секрет → env `CLOUDFLARE_PROXY_SECRET` (prod+preview) +
     Transform Rule зоны CF: заголовок `x-cf-proxy-secret` на все запросы.
  5. Денежный/ботовый пути мимо CF: Telegram-webhook перерегистрировать на
     `oplati-podpisku-web.vercel.app/api/bot` (домен идёт напрямую на Vercel;
     Telegram-серверам блокировки РФ безразличны); для webhook L&P — WAF
     skip-правило на `/api/payments/loveandpay` (страховка — cron
     `poll-payment` дожимает потерянные вебхуки за ≤5 мин), Bot Fight Mode
     не включать.
  6. Оранжевое облако на оба CNAME (TXT `_loveandpay` не трогать — верификация
     L&P не зависит от прокси; IP-whitelist L&P — исходящий путь через VPS
     `177.7.34.106`, CF его не касается).
  7. Смоук: check-host с RU-узлов + телефон владельца без VPN, тестовая
     оплата e2e, `getWebhookInfo` бота, Mini App из бота.
- **Вариант беты без риска для прода:** поддомен `beta.oplatishka.com`
  (оранжевое облако, привязан к preview-ветке в Vercel) — проверить доступ из
  РФ без VPN до переключения www/apex.
- **Откат:** серое облако обратно — трафик мгновенно идёт напрямую на Vercel.

## Код (мелкое, из аудита 2026-07-18)

- **L-1** — фильтр `original_currency='USD'` в суммах оборота рефералки
  (`referral-progression.ts`, `referral-cabinet.ts`): не-USD заказы сейчас
  завышали бы оборот (guard в accrue-пути есть, в витрине/прогрессии — нет).
- **T-5** — тесты `splitForTelegram`/`tokenizeForSplit` (разбивка длинных
  сообщений по 4096 с code-блоками) и link-handoff.
- **tool-cards.ts → Zod** (находка CodeRabbit 2026-07-19): самописная валидация
  tool-call payload'ов в веб-чате → схемы из `@oplati/types`.
- **Фикстуры аудита прибраны, но:** ключи `env.YOOKASSA_SECRET_KEY` /
  `env.CRYPTOBOT_TOKEN` остались в redact-листе `lib/logger.ts` — безвредно,
  убрать при следующей правке логгера.

## Продукт / стратегическое

- **Рефералка, Этап E2** — реальный исполнитель выплат (сейчас mock, денег не
  двигает): решить, чем платим (payout-API L&P? сеть USDT?), подключить
  `settlePayout`, формы реквизитов в UI. **E3** — антифрод выводов.
- **VCC-preflight крупных заказов** — перед заказом до $5000 (Airbnb/Booking)
  баланс VCC не проверяется: `createCard`/`topup` падает уже ПОСЛЕ приёма
  рублей → заказ в `failed` и ручной возврат. Добавить проверку баланса на
  `propose_order` для high-value сумм.
- **Поддержка → Telegram forum-topics** — целевая схема вместо interim-DM
  (один topic = один заказ, `/ai_back` возвращает AI).
- **Retention: подписка Supabase** — план владельца 2026-07-19: при росте
  данных перейти на платный тариф; cron `retention` уже ограничивает рост.
