# Бэклог — отложенные задачи и идеи

**Единственный список «на потом».** Всё, что решено не делать сейчас: отложенные
задачи, открытые находки аудитов, идеи по продукту. Прод работает без них.

Правила ведения:
- новая запись — с **датой** и ответом на «почему отложено» (без этого через месяц
  непонятно, отложено осознанно или забыто);
- закрытая — убирается отсюда и попадает в [`CHANGELOG.md`](CHANGELOG.md);
- если запись описывает контур, которого больше нет, — её надо удалить, а не
  оставлять «на всякий случай»: устаревшая запись хуже отсутствующей, потому что
  агент (и человек) читает её как актуальную.

Закрытые аудиты и исполненные планы — в [`history/`](history/).

---

## Безопасность

### `CRON_SECRET` лежит открыто на VPS (S-3 проверки 2026-07-25)

`/etc/cron.d/oplatishka` содержит секрет открытым текстом, и cron пишет **всю
командную строку вместе с `Authorization: Bearer <секрет>` в `/var/log/syslog`**
при каждом запуске. Секрет также виден в `ps` / `/proc/<pid>/cmdline`. Владелец
секрета может дёрнуть `retention` (удаление сообщений) и `recycle-cards`
(необратимый release карт = деньги).

- **Смягчает:** `/opt/alloy/config.alloy` файлы `/var/log` не собирает — в Loki
  секрет не уезжает. Экспозиция ограничена локальными процессами VPS.
- **Что делать:** вынести секрет в отдельный `600`-файл и звать
  `curl --config /etc/oplatishka/cron.conf`; `/etc/cron.d/oplatishka` → `chmod 600`.
  Заготовка и обоснование — в шапке [`../infra/crontab.example`](../infra/crontab.example).
- **Почему не сделано сразу:** правка на живом VPS, а не в репозитории; чтобы
  файл и репозиторный пример не разъехались, менять надо оба одновременно.

### Приложение подключается к Postgres суперюзером (S-5 / C-3, 2026-07-25)

Роль `oplatishka` — `rolsuper=t`, `rolbypassrls=t` (так создаёт образ
`postgres:17`). Утечка `DATABASE_URL` (панель Dokploy, дамп в R2, лог) даёт не
scoped-роль, а полный суперюзер: можно `ALTER TABLE order_events DISABLE TRIGGER`,
то есть снять DB-уровневую защиту инварианта №1, и `COPY … FROM PROGRAM`. На
Supabase таких прав у приложения не было.

- **Что делать:** отдельная роль `NOSUPERUSER BYPASSRLS` с GRANT только на
  `public`; суперюзер оставить для миграций и `db:init-roles`.
- **Риск правки:** смена `DATABASE_URL` на проде + прогон миграций под другой
  ролью. Делать на dev-стенде, потом на проде.

### Репозиторий публичный, а в доках инфра-хосты (S-6 / C-4, 2026-07-25)

`elfuerte72/oplati_podpisku` — `visibility: PUBLIC`. В доках светятся URL панели
Dokploy, внутренние хосты сервисов, логин Basic Auth dev-стенда, имя R2-бакета.
Токенов не закоммичено (проверено), но адрес панели, компрометация которой = деплой
произвольного кода и все прод-секреты, отдаётся бесплатно.

- **Что делать:** панель за IP-allowlist или VPN; инфра-хосты вынести из
  публичного репозитория.

### CSP → боевой режим (L-21 аудита; отложено 2026-07-19)

CSP с 2026-07-11 работает в `Report-Only`. Жалоб за период наблюдения не было.
С 2026-07-25 `report-uri` снова доставляет отчёты в Sentry (до пересборки DSN не
попадал в билд, отчёты уходили в никуда) — то есть наблюдение опять живое.

- **Что делать:** в `apps/web/next.config.ts` заменить
  `Content-Security-Policy-Report-Only` на `Content-Security-Policy`.
- **Перед включением:** просмотреть CSP-репорты в Sentry за неделю; после —
  смоук сайта (чат, каталог, оплата, партнёрка). Откат — вернуть `-Report-Only`.
- ⚠️ **Проверить баннер Freekassa в футере.** Он тянется с `cdn.freekassa.net`;
  домен уже внесён в `img-src`, но при любой правке политики это надо сохранить —
  пропавшая картинка для Freekassa равна снятому баннеру, то есть нарушению
  условий подтверждения ресурса.

### `CLIENT_IP_MODE` — безопасный режим opt-in (C-2, 2026-07-25)

Дефолт `'vercel'`, то есть на self-host правильный режим надо задать вручную, и
рантайм не проверяет, что его задали. Пересоздали приложение или потеряли env →
`curl -H 'x-real-ip: <random>'` обнуляет per-IP лимит на каждом запросе (CWE-348),
без единого алёрта.

- **Что делать:** дефолт по признаку платформы (`VERCEL === '1' ? 'vercel' :
  'traefik'`) либо fail-fast / Sentry-warn при
  `NODE_ENV=production && !VERCEL && CLIENT_IP_MODE==='vercel'`.

### Sentry-алёрты в Telegram не подключены (S-11, отложено владельцем 2026-07-25)

В правиле Sentry «Send a notification for high priority issues» есть только
действие **Email**. Webhook-действия нет вообще, поэтому эндпоинт
`/api/alerts/sentry` и заданный на проде `SENTRY_ALERT_WEBHOOK_SECRET` ни к чему
не подключены — «подставить секрет» нечего, действие надо создать.

- **Что это даёт:** пуш в `@oplatishkaAlert_bot` по **непредусмотренным** ошибкам.
  Денежные случаи (недоплата, сбой выпуска карты, недоступность прокси) и так
  долетают напрямую через `notifyOps`, минуя Sentry; ошибки в Sentry пишутся и
  уходят письмом. То есть это расширение охвата, а не восстановление сломанного.
- **Что делать (только UI, инструмента на изменение правил в Sentry MCP нет):**
  Alerts → правило 644412 → добавить действие «Send a notification via a webhook»
  с URL `https://www.oplatishka.com/api/alerts/sentry?s=<SENTRY_ALERT_WEBHOOK_SECRET>`.
- **Если решено, что письма достаточно:** убрать `SENTRY_ALERT_WEBHOOK_SECRET` из
  env прода, чтобы неподключённый секрет не выглядел недоделкой.

---

## Инфраструктура

### Внешний uptime-мониторинг VPS (отложено 2026-07-19, актуализировано 2026-07-25)

После переезда на Dokploy весь контур — один VPS `177.7.34.106` (сайт, бот, БД,
Redis, egress к L&P). Внутренние healthcheck'и живут на нём же, то есть при его
падении сообщить о падении некому. Нужен независимый «второй глаз».

- **Что делать (не код, ~10 минут):** UptimeRobot (free) → Add Monitor тип HTTPS
  на `https://www.oplatishka.com/api/health`, интервал 5 мин, alert-контакт email
  или телеграм. **За владельцем.**
- **Устарело:** прежняя формулировка про порт squid-прокси `24128` — приложение
  больше не ходит к L&P через squid (egress прямой с задекларированного IP VPS).

### Cron-джобы: нет heartbeat'а (хвост B-3/E-1, 2026-07-25)

Сбой cron теперь виден в syslog (`logger -t oplatishka-cron`), но никто не
заметит, если джоб перестанет запускаться **вовсе** (снесли `/etc/cron.d`, лежит
cron, лежит VPS). `poll-payment` — единственный backstop потерянных webhook'ов L&P.

- **Что делать:** healthchecks.io или UptimeRobot «Heartbeat»: джоб пингует URL
  при успехе, отсутствие пинга дольше N минут → алёрт.

### Возврат бот-webhook на `www` (хвост переезда)

Webhook бота стоит на `new.oplatishka.com` — тот же контейнер, работает, но
семантически временно (так обошли DNS-кэш Telegram при cutover). Переставить на
`www`, когда захочется чистоты: `getWebhookInfo` должен показать ip
`177.7.34.106` для www.

### Остывание резерва отката

Пока живы и стоят денег: **Vercel-проект** (автодеплой отключён, домены сняты,
кроны погашены WAF-правилом), **Supabase-прод** `nyxijwpuvctmvemaemqn` (данные на
момент cutover), **Timeweb-VPS** `104.171.133.70` (не в пути трафика),
**squid-прокси** `:24128` (не в пути платежей), **Upstash** (заменён self-host
Redis+SRH).

- **Держать:** Supabase ≥ месяц (страховка данных), Vercel 1–2 недели.
- **Порядок гашения и что именно снимать** — [`runbooks/rollback.md`](runbooks/rollback.md).
- **Не удалять ничего** до явного решения владельца.

### PITR для БД (RPO сейчас 24 ч)

Бэкап — суточный дамп в R2 (03:00 UTC, keep 14), проверен восстановлением
2026-07-25. Между дампами потеря до суток заказов и платежей.

- **Что делать при реальном трафике:** WAL-архивация (wal-g) или managed-Postgres
  с PITR. Процедура текущего восстановления —
  [`runbooks/backup-restore.md`](runbooks/backup-restore.md).

### SPOF: приложение и БД на одном VPS

При росте — вынести Postgres на отдельную ноду. Целевая архитектура описана в
[`history/dokploy-migration-plan.md`](history/dokploy-migration-plan.md).

### `Alt-Svc: h3` вернулся (S-7, 2026-07-25)

На схеме с Timeweb заголовок специально снимали middleware'ом — ТСПУ режет QUIC.
Новый Traefik его рекламирует снова. VPS доступен из РФ напрямую, так что скорее
всего безвредно; но если у кого-то сайт «моргает» на мобильном — начинать отсюда.

### Нет graceful shutdown (E-6, 2026-07-25)

`node` — PID 1 без init; standalone-сервер Next 16 на SIGTERM делает
`process.exit(143)` без дренажа. Rolling-update во время POST
`/api/payments/loveandpay` рвёт соединение, заказ остаётся в `paid`; спасает
`poll-payment` в течение 5 минут.

- **Что делать:** `--init` у контейнера + обработчик SIGTERM с дренажом, либо
  `stopGracePeriodSwarm` и вынос фулфилмента из синхронного пути вебхука.

### Мелкое по инфраструктуре

- **A-2** — рунбук восстановления сверяет COUNT'ы, но не наличие триггера
  `order_events_append_only`, RLS-политик и журнала `drizzle`. Проверки добавлены
  в [`runbooks/backup-restore.md`](runbooks/backup-restore.md) — осталось
  прогонять их при каждом учении.
- **A-3** — `HEALTHCHECK` не трогает БД, а `DATABASE_URL` в env-схеме
  `optionalUrl()`: опечатка в хосте (имя `oplatishka-db-ry3smb` меняется при
  пересоздании сервиса) → контейнер `healthy`, релиз докатывается, путь
  заказ→оплата падает в рантайме.
- **A-5** — дамп с `--no-acl` не переносит `GRANT SELECT ON services TO anon,
  authenticated` из миграции 0010, а повторно миграция не прогонится. Сейчас
  инертно (роли `NOLOGIN`, anon-клиента нет), направление fail-closed.
- **D-4** — `Dockerfile` перечисляет манифесты поимённо вразрез с глобами
  `pnpm-workspace.yaml`: добавили `packages/<новый>` → `pnpm install
  --frozen-lockfile` падает.

---

## Код (мелкое, из аудитов)

- **L-1** — фильтр `original_currency='USD'` в суммах оборота рефералки
  (`referral-progression.ts`, `referral-cabinet.ts`): не-USD заказы завышали бы
  оборот (guard в accrue-пути есть, в витрине/прогрессии — нет).
- **T-5** — тесты `splitForTelegram`/`tokenizeForSplit` (разбивка сообщений по
  4096 с code-блоками) и link-handoff.
- **tool-cards.ts → Zod** (CodeRabbit 2026-07-19): самописная валидация
  tool-call payload'ов в веб-чате → схемы из `@oplati/types`.
- **B-2** — дедуп `renewal_reminder_sent` — read-then-write, не атомарный. При
  одновременном запуске двух джобов клиент получит напоминание дважды.
- **B-4** — `SELF_BASE_URL` безусловно приоритетнее и не проверяется на «свой
  хост»: ошибочное значение создаст инвойс в чужом стеке и отдаст
  `X-Internal-Token` промежуточным прокси.
- **C-5** — `init-roles.ts` создаёт локальный `pino` в обход `lib/logger.ts` с его
  redact-листом: `logger.error({ err })` сериализует ошибку `postgres`-клиента
  без скраба (детали соединения → stdout → Loki).
- **E-9** — роли задаются дважды и расходятся: `service_role … BYPASSRLS` в
  `init-roles.ts`, без него в PGlite-тестах → тест проверяет не тот контур, что
  едет в прод.
- **Redact-лист логгера** — ключи `env.YOOKASSA_SECRET_KEY` / `env.CRYPTOBOT_TOKEN`
  остались от удалённых провайдеров; безвредно, убрать при следующей правке.

---

## Продукт / стратегическое

- **Рефералка, Этап E2** — реальный исполнитель выплат (сейчас mock, денег не
  двигает): решить, чем платим (payout-API L&P? сеть USDT?), подключить
  `settlePayout`, формы реквизитов в UI. **E3** — антифрод выводов.
- **VCC-preflight крупных заказов** — перед заказом до $5000 (Airbnb/Booking)
  баланс VCC не проверяется: `createCard`/`topup` падает уже ПОСЛЕ приёма рублей
  → заказ в `failed` и ручной возврат. Добавить проверку баланса в `propose_order`
  для high-value сумм. Актуально: баланс VCC на 2026-07-25 — $104.60 при пороге
  алёрта $50, то есть запас в пару заказов.
- **Поддержка → Telegram forum-topics** — целевая схема вместо interim-DM (один
  topic = один заказ, `/ai_back` возвращает AI).
- **`TELEGRAM_MINIAPP_SHORTNAME`** — не задан, поэтому кнопка «Личный кабинет» на
  сайте открывает кабинет через `?start=cabinet`-меню, а не прямой ссылкой (лишний
  тап). Значение живёт в @BotFather → Bot Settings; через Bot API не читается.
- **Повторный $4 за выпуск карты** — закрыто 2026-07-25 (срок жизни карты стал
  одним числом, долив без надбавки весь срок). Оставлено как ориентир: если
  экономика изменится, точка настройки — `CARD_LIFETIME_DAYS` в
  `@oplati/types/card-lifecycle`.
