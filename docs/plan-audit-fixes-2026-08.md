# План исправлений по аудиту 2026-08-10

**Что это.** Рабочее ТЗ на цикл починки находок полного аудита (`/full-audit`,
2026-08-10). Полный отчёт с обоснованиями, сценариями отказа и опорой в коде —
`AUDIT-2026-08-10.md` в корне репозитория (untracked, **не коммитить**). Этот
файл — план работ поверх него: что чинить, в каком порядке, чем закрывать.

**Исходные данные аудита.** 77 агентов по 12 осям, 138 сырых находок → 120
уникальных + 6 из добора → **46 подтверждённых** (6 HIGH, 35 MEDIUM, 5 LOW).
Блокеров нет. Baseline на момент аудита зелёный: `pnpm typecheck` чистый, тесты
web 695 / types 171 / db 60.

---

## Статус (обновлять по мере работы)

Отмечать пачку сделанной **после мержа PR**, а не после написания кода. Сессия,
берущая новую пачку, сначала читает эту таблицу; если она разошлась с
реальностью — сверить по `docs/CHANGELOG.md` и `git log --oneline -20`.

| Пачка | Тема | Статус | PR |
|---|---|---|---|
| 1 | Тихая потеря денег (HIGH) | коммит `c04162a` в `fix/audit-2026-08` | — |
| 2 | Цена и конфиг денег | коммит в `fix/audit-2026-08` | — |
| 3 | Контракт Freekassa и nonce | не начата | — |
| 4 | Секреты и PII | не начата | — |
| 5 | Rate-limit и анти-абьюз | не начата | — |
| 6 | Устойчивость клиентов провайдеров | не начата | — |
| 7 | Бот: дедуп и деградация | не начата | — |
| 8 | AI-контур | не начата | — |
| 9 | Рефералка (нужно решение владельца) | не начата | — |
| 10 | Cron и данные каталога | не начата | — |
| 11 | Тесты: денежные пути | не начата | — |
| 12 | Тесты: авторизация и CI-гейты | не начата | — |
| 13 | Тесты: остальное покрытие | не начата | — |

---

## Как пользоваться этим файлом

Одна сессия = **одна пачка** = один PR. Пачки отсортированы по риску: 1–3 —
тихая потеря денег, 4–5 — безопасность и абьюз, 6–8 — устойчивость, 9–10 —
рефералка и данные, 11–13 — тесты. Порядок можно менять, но пачку 1 стоит взять
первой: обе её находки означают, что клиент платит и остаётся без результата, а
мы об этом не узнаём.

Внутри пачки находки подобраны так, чтобы правки лежали рядом и закрывались
общим набором тестов, — это осознанно, чтобы PR оставался обозримым.

---

## Правила работы (действуют для КАЖДОЙ пачки)

1. **Сначала перепроверь находку по коду, потом чини.** Верификация аудита не
   опровергла ни одной находки из 45 — сигнал слабый, поэтому презумпции
   правоты у них нет. Открой указанный `file:line`, прочитай функцию целиком и
   вызывающий код. Если находка не подтверждается — **не чини**, запиши в PR,
   почему она неверна. Две находки (пачка 1) уже перепроверены вручную при сдаче
   аудита и подтверждены.
2. **Регрессионный тест обязателен на каждый фикс.** Тест должен падать на старом
   коде и проходить на новом. Без теста фикс не считается сделанным: половина
   находок — это ровно те места, где «зелёный CI при сломанном инварианте».
3. **Границы прогона.** Не ходить на прод и в боевую БД, не дёргать внешние API.
   Всё проверяется локально: `pnpm typecheck`, `pnpm --filter web test`,
   `pnpm --filter @oplati/types test`, `pnpm --filter @oplati/db test` (PGlite,
   БД не нужна).
4. **Инварианты проекта из `CLAUDE.md` не нарушать** — особенно: переходы статусов
   только через `transitionOrder()`, `order_events` append-only, деньги — целые
   в минимальных единицах, Zod на всех границах, вебхуки всегда 200 (кроме
   `/api/bot` 401), никаких `console.log` и `catch {}`, `fetch` только с таймаутом.
5. **Пайплайн.** Ветка от `main` → PR → зелёные `Tests`/`Type Check`/`Lint`/
   `Build`/`Secret Scan`/`Dependency Review` → squash. Прямой push в `main`
   закрыт ruleset'ом. Коммиты — Conventional Commits, заголовок ≤ 72 символов.
6. **Миграции.** Ни одна пачка ниже миграции не требует. Если она всё же
   понадобится — forward-only через Drizzle, и помнить: **деплой миграции не
   применяет**, это отдельный ручной шаг после мержа.
7. **`docs/CHANGELOG.md` — вести обязательно.** Запись на каждую пачку.
8. **Если фикс упирается в продуктовое решение — остановись и спроси.** Такие
   места помечены ниже как «нужно решение владельца». Не выбирать за владельца.
9. **Перед merge — `/full-review`** на ветке (скилл в репозитории). Аудит нашёл
   проблемы; ревью проверяет, что фикс их действительно закрыл и не привёз новых.

---

## Пачка 1 — тихая потеря денег (HIGH)

**Риск:** клиент заплатил, результата нет, система молчит. Обе находки
перепроверены вручную.

| Находка | Что сделать |
|---|---|
| `apps/web/lib/jobs/expire-payments.ts:53` (HIGH) — захоронение pending-платежа без финальной сверки с провайдером. `findPendingPaymentsForPoll` (`packages/db/src/repositories/payments.ts:238`) фильтрует строго `status='pending'`, поэтому захороненный платёж не опрашивается **никогда**. Оплата на последних минутах жизни счёта + потерянный вебхук = деньги у провайдера, заказ `expired`, ноль алёртов. | Перед `claimPaymentTerminal` опросить провайдера по pending-платежу (переиспользовать `pollOne` из `poll-payment`). Оплачен → `processInvoicePaid`/`processFreekassaPaid` и **не** хоронить. Не оплачен → хоронить как сейчас. |
| `packages/db/src/repositories/cards.ts:92` (HIGH) — `findActiveByUserId` единственная из card-выборок **без** возрастного фильтра (в `findCardsByUserIdForCabinet:120`, `findCardByIdForUser:150`, `findCardsToRecycle:257` он есть). Карта старше `CARD_LIFETIME_DAYS` доливается деньгами клиента и закрывается ночным recycle — остаток уходит на наш VCC. | Добавить в `findActiveByUserId` тот же отсекатель по возрасту, с запасом в сутки: `createdAt >= now() - make_interval(days => CARD_LIFETIME_DAYS - 1)`. Тогда карта в последний день жизни не доливается, а `issue-card` выпускает новую. Проверить второго потребителя — `propose-order.ts:257` (решение о надбавке $4). |
| `apps/web/lib/jobs/expire-payments.ts:51` (MEDIUM) — цикл работает по снапшоту `findExpiredPayableOrders` и не перечитывает заказ перед claim'ом: конкурентный `confirm` мог продлить `expires_at`, пока шёл внешний вызов шлюза. | В цикле перед claim'ом перечитать заказ и пропустить, если статус вышел из оплатимых или `expires_at` уехал в будущее. Сжимает окно с минут до миллисекунд. |

**Тесты:** PGlite — захоронение НЕ трогает платёж, который провайдер показывает
оплаченным; `findActiveByUserId` не возвращает карту старше срока (и возвращает
свежую); ревалидация пропускает заказ с продлённым `expires_at`.
**Приёмка:** заказ с оплатой в последнюю минуту TTL и потерянным вебхуком
доходит до `paid`; карта в последний день жизни не доливается.
**Коммит:** `fix(payments): не терять оплату при захоронении счёта и не доливать протухшую карту`

⚠️ Заодно поправить устаревший комментарий `payments.ts:223-229` — он обещает
«страховку 25 часов, TTL invoice 24h», тогда как TTL фактически 1 час.

---

## Пачка 2 — цена и конфиг денег

| Находка | Что сделать |
|---|---|
| `apps/web/lib/env.ts:319` (MEDIUM) — пустая строка `KEY=` обходит дефолты Zod-схемы: `COMMISSION_PERCENT=` даёт комиссию **0%**, `FREEKASSA_MAX_AMOUNT_RUB=` снимает потолок суммы. С учётом того, что правка env в Dokploy перезаписывает его целиком, одна опечатка = работа в убыток без сигнала. | Пустую строку трактовать как «не задано»: препроцессинг `v => v === '' ? undefined : v` перед схемой (или общий хелпер для всех числовых/процентных env). Проверить ВСЕ env, где есть дефолт. |
| `apps/web/lib/rapira/rates.ts:64` (MEDIUM) — курс принимается без предела правдоподобия; сдвиг порядка величины у провайдера молча фиксирует цены на 2 часа. | Границы разумности (например, 40–200 ₽ за USDT) в env или константе: вне диапазона → fallback + Sentry-алёрт, а не принятие. |
| `apps/web/lib/tool-handlers/propose-order.ts:272` (MEDIUM) — пол суммы зашит на `LOVEANDPAY_MIN_AMOUNT_RUB`, а гейт `confirm` и текст клиенту — по минимуму **текущего** шлюза. Разъезд env даёт непроходимые заказы с неверной цифрой в тексте. | Один источник: минимум брать у активного шлюза (`lib/payments/gateway.ts`), и в проверке, и в тексте. |
| `apps/web/lib/cabinet/read.ts:272` (MEDIUM) — `buyerFeePercent` берётся от текущего шлюза, а не от провайдера уже выставленного счёта. | Брать провайдера из самого платежа. Сейчас на проде `FREEKASSA_BUYER_FEE_PERCENT=0`, поэтому не стреляет, — но при возврате комиссии на покупателя это враньё о цене. |

**Тесты:** `env.test.ts` — пустая строка даёт дефолт для каждого env с дефолтом;
курс вне границ → fallback + алёрт; минимум суммы следует за `PAYMENT_PRIMARY_PROVIDER`.
**Коммит:** `fix(pricing): пустой env не отключает дефолты, курс и минимум суммы — из одного источника`

---

## Пачка 3 — контракт Freekassa и nonce

| Находка | Что сделать |
|---|---|
| `apps/web/lib/freekassa/handlers.ts:94` (MEDIUM) — платёж ищется по `intid`, который **не входит** в MD5-подпись, и найденный платёж не сверяется с подписанным `MERCHANT_ORDER_ID`. | После поиска по `intid` сверять `providerInvoiceNumber === merchantOrderId`; расхождение → не кредитовать, Sentry error, ответ не-`YES` (провайдер повторит). Симметрично обратной сверке, которая уже есть в fallback-ветке. |
| `apps/web/lib/payments/gateway.ts:285`, `apps/web/lib/freekassa/client.ts:84`, `apps/web/lib/jobs/poll-payment.ts:276` (MEDIUM ×3, **одна проблема**) — монотонность nonce обеспечена только внутри cron-пула; конкурентные `createOrder` из `payments/create` и из `poll-payment` могут приехать к провайдеру в обратном порядке и быть отвергнуты. | Сериализовать выдачу+отправку: advisory-lock на время запроса либо единая очередь запросов к Freekassa. Проверить, что `createOrder` по-прежнему **не** ретраится. |

**Тесты:** уведомление с чужим `intid` при валидной подписи не кредитует платёж;
два конкурентных `createOrder` уходят в порядке возрастания nonce.
**Коммит:** `fix(freekassa): сверять подписанный номер заказа и сериализовать nonce`

---

## Пачка 4 — секреты и PII

| Находка | Что сделать |
|---|---|
| `apps/web/lib/telegram/templates.ts:379` (MEDIUM) — DM оператору из `/support` уходит **без** `redactCardNumbers`: если клиент напишет номер карты в поддержку, полный PAN доставляется в чат. | Прогнать текст клиента через тот же redact, что уже применяется в `buildPaymentIssueOperatorMessage`. |
| `apps/web/lib/freekassa/errors.ts:21` (MEDIUM) + тот же дефект у L&P — `rawBody` объявлен перечисляемым свойством, хотя тело ответа `/orders` может содержать полный PAN, а ошибки сериализуются в pino/Sentry. | Сделать `rawBody` неперечисляемым — как уже сделано у `PaySpaceContractError`. |
| `apps/web/lib/sentry.ts:86` (LOW, подтверждена) — `beforeSend` не скрабит `event.contexts` и `event.tags`, хотя комментарий заявляет обратное. | Дописать скраббинг либо привести комментарий в соответствие коду. |
| `apps/web/app/api/payments/loveandpay/route.ts:54` (LOW, подтверждена) — при отсутствии `LOVEANDPAY_WEBHOOK_SECRET` вебхук отвечает `200 ok` и молча глотает платёжное событие: только `log.warn`, без Sentry. Провайдер считает доставку успешной и не ретраит. Симметричный путь Freekassa (`freekassa/route.ts:70-80`) в том же кейсе шлёт Sentry error и просит повтор. | Сравнять поведение с Freekassa: `Sentry.captureMessage(level: 'error')`. Ответ оставить 200 (инвариант 6). |

**Тесты:** canary — PAN-подобная последовательность в тексте поддержки не доходит
до сообщения оператору; `rawBody` не появляется в сериализации ошибки.
**Коммит:** `fix(security): маскировать PAN в поддержке и убрать rawBody из сериализации`

---

## Пачка 5 — rate-limit и анти-абьюз

| Находка | Что сделать |
|---|---|
| `apps/web/lib/telegram/handle-update.ts:143` (MEDIUM) — `/start` (включая `ref_`/`link_`), `/menu` и медиа-сообщения обходят per-identity rate-limit бота: неограниченные записи в БД с одного `telegram_id`. | Провести эти пути через тот же `checkRateLimit`, что и текстовые сообщения. Осторожно с `/start link_*` — он часть живого флоу привязки, лимит должен быть щадящим. |
| `apps/web/app/api/cabinet/route.ts:100` (MEDIUM) — upsert пользователя и реферальный захват выполняются **до** rate-limit, IP-барьера нет вовсе (в отличие от `/api/cabinet/referral`). | Барьер по IP до резолва пользователя — как требует инвариант 9 `CLAUDE.md`. |
| `apps/web/app/api/analytics/route.ts:99` (LOW, подтверждена) — роут доверяет клиентскому `channel='miniapp'` без подписанной `initData`, вопреки собственному контракту. | Принимать `miniapp` только при валидной подписи, иначе — понижать до web. |

**Тесты:** серия `/start` с одного `telegram_id` упирается в лимит; `/api/cabinet`
без валидной подписи не создаёт строку в `users`.
**Коммит:** `fix(ratelimit): закрыть обходные пути бота и кабинета`

---

## Пачка 6 — устойчивость клиентов провайдеров

| Находка | Что сделать |
|---|---|
| `apps/web/lib/pay-space/client.ts:457` и `apps/web/lib/loveandpay/client.ts:152` (MEDIUM ×2) — `clearTimeout` вызывается до `resp.text()`: чтение тела не под таймаутом и может висеть бесконечно. | Держать `AbortController` до конца чтения тела. Проверить заодно `remnawave` и `rapira`. |
| `apps/web/lib/pay-space/client.ts:473` (MEDIUM) — 5xx/429 с JSON-телом без поля `success` не ретраится даже у идемпотентных операций. | Классификатор ретрая должен смотреть на HTTP-статус, а не только на конверт. `createCard` по-прежнему **не** ретраить. |
| `apps/web/lib/jobs/issue-card.ts:212` (MEDIUM) — транзиентный 429/5xx на топ-апе трактуется как «провайдер отклонил»: живая карта уводится в `idle`, клиенту дорожает следующий заказ на $4. | Различать доменный отказ (`success:false`, `topup_failed`) и транспортный сбой: `markIdle` только для первого, для второго — оставить карту активной и уронить заказ в `failed` с алёртом. |

**Тесты:** висящее тело ответа обрывается по таймауту; 503 у идемпотентной
операции ретраится; 429 на топ-апе НЕ уводит карту в `idle`.
**Коммит:** `fix(providers): таймаут на чтение тела, ретрай 5xx и разбор природы отказа топапа`

---

## Пачка 7 — бот: дедуп и деградация

| Находка | Что сделать |
|---|---|
| `apps/web/app/api/bot/route.ts:87` + `apps/web/lib/telegram/handle-update.ts:72` (MEDIUM ×2, одна проблема) — нет дедупа `update_id`: апдейт обрабатывается синхронно до 90 с, а Telegram при таймауте передоставляет его — повторные побочные эффекты и (при включённом AI) двойной расход. | Дедуп по `update_id` с коротким TTL. Хранилище: Redis (он уже есть под rate-limit) либо таблица. Не in-memory — оно не переживёт вторую реплику. |
| `apps/web/app/api/bot/route.ts:104` (MEDIUM) — отсутствие `ANTHROPIC_API_KEY` молча выключает **весь** бот, включая платёжно-критичные не-AI флоу (`/start`, меню, оплата, VPN). | Гейт по ключу должен закрывать только AI-путь; кнопочные и платёжные флоу обязаны работать без Anthropic. |

**Тесты:** повторная доставка того же `update_id` не вызывает обработчик дважды;
при пустом `ANTHROPIC_API_KEY` `/start` и меню отвечают штатно.
**Коммит:** `fix(bot): дедуп update_id и работа кнопочных флоу без ключа Anthropic`

---

## Пачка 8 — AI-контур

Практическая срочность низкая: `BOT_AI_ENABLED` и `WEB_AI_ENABLED` на проде
выключены. Но чинить **до** включения AI, иначе защита расходов не работает
именно тогда, когда впервые понадобится.

| Находка | Что сделать |
|---|---|
| `packages/agent/src/index.ts:393` (HIGH) — при `throw` из tool-loop накопленный usage теряется целиком: дневной бюджет слеп к самым дорогим запросам (6 итераций Sonnet записываются как 0 токенов). | Типизированная `AgentLoopError` с полем `usage`; в обоих call-site (`app/api/chat/route.ts`, `lib/telegram/agent-dialog.ts`) в `catch`/`finally` звать `recordAgentUsage`. |
| `packages/agent/src/index.ts:340` и `:384` (MEDIUM ×2) — `stop_reason` `pause_turn` (серверный `web_search`) и `max_tokens` не обрабатываются: частичный или пустой ответ выдаётся как финальный, в боте — молчание. | Обработать оба: `pause_turn` — продолжить ход, `max_tokens` — явный ответ пользователю вместо тишины. |

**Тесты (новые — у `packages/agent` тестов нет вообще):** падение на 2-й итерации
→ `recordAgentUsage` вызван с usage первой; `pause_turn` не завершает ход.
**Коммит:** `fix(agent): учитывать usage при сбое tool-loop и обрабатывать pause_turn`

⚠️ Здесь же завести `packages/agent/vitest.config.ts` и test-скрипт — см. пачку 12.

---

## Пачка 9 — рефералка ⚠️ нужно решение владельца

| Находка | Что сделать |
|---|---|
| `packages/db/src/repositories/link-tokens.ts:163` (HIGH) — самореферал: свой web-код, открытый в боте, создаёт вторую строку `users` с `referred_by` = первая; merge при привязке переносит начисления на того же человека. Проверка `referrerId !== userId` не срабатывает — строки разные. | **Решение владельца:** гасить ли уже начисленное. Код-часть: в транзакции merge вставлять компенсирующие строки `status='reversed'` там, где `beneficiary_user_id = source_user_id` (ledger append-only, удалять нельзя). Плюс разовый отчёт по ledger'у: есть ли такие строки уже. |
| `apps/web/lib/referral/accrue.ts:93` + `packages/types/src/referral.ts:141` (MEDIUM ×2) — ставка начисления берётся из таблицы по `circle`, а кабинет показывает `locked_rate_l1_bps`: два источника правды вопреки комментарию. | **Решение владельца:** какая ставка главная — зафиксированная при регистрации или текущая по статусу. Затем убрать второй источник. |
| `apps/web/lib/cabinet/referral-actions.ts:107` (MEDIUM) — заявка на вывод создаётся из живого UI, никого не уведомляет и ничем не исполняется: баланс партнёра молча замораживается навсегда (`settlePayout` — mock, нигде не вызывается). | Минимум до реальных выплат: DM владельцу о новой заявке + честный текст партнёру о сроках. Либо закрыть кнопку флагом, пока E2 не сделан. |

**Коммит:** `fix(referral): погасить самоначисления при merge и свести ставку к одному источнику`

---

## Пачка 10 — cron и данные каталога

| Находка | Что сделать |
|---|---|
| `apps/web/lib/jobs/referral-rollup.ts:50` (MEDIUM) — джоб не добирает пропущенный месячный запуск: простой VPS в 02:00 UTC 1-го числа теряет месяц прогрессии навсегда. | Добор: при запуске проверять последний обработанный месяц в `referral_monthly_stats` и догонять пропущенные. Идемпотентность уже обеспечена `PK(user_id, month)`. |
| `packages/db/scripts/seed-catalog.ts:594` (LOW, подтверждена добором) — гейт уникальности тарифов ключён на `(period, originalAmount)`, а веб/кабинет-матчинг ищет по `(name, period)`: дубль по имени+периоду даёт неверную цену и не ловится сидом. | Привести ключ гейта к `(name, period)` — тому, по которому реально идёт матчинг. |

**Тесты:** пропущенный месяц догоняется и не дублируется при повторе; seed падает
на дубле `(name, period)`.
**Коммит:** `fix(cron): добор пропущенной месячной прогрессии и гейт уникальности тарифов`

---

## Пачки 11–13 — тесты (ось L)

Отдельный блок: это не правки поведения, а закрытие дыр, из-за которых
«зелёный CI при сломанном инварианте». Две находки уровня HIGH.

**Пачка 11 — денежные пути без тестов**
- `apps/web/app/api/bot/route.ts:51` (HIGH) — единственный auth-барьер бота
  (401 на неверный secret-token) и разбор апдейтов не покрыты ничем. Route-тест
  по образцу `payments/freekassa/route.test.ts`: неверный секрет → 401 и
  обработчик не зван; битый JSON → 200 skipped; не-Zod апдейт → 200 skipped;
  обработчик бросил → всё равно 200.
- `apps/web/lib/tool-handlers/confirm-order.ts:157` (HIGH) — денежный путь №1:
  гейт привязки Telegram, ownership, классификация ошибок 503/409/422. Юнит-тест
  с моками и `vi.stubGlobal('fetch')`.
- `apps/web/app/api/payments/loveandpay/route.ts:76` (MEDIUM) — route-тест на
  подпись: она единственный барьер от поддельного `invoice.paid`.

**Пачка 12 — авторизация и CI-гейты**
- `apps/web/app/api/cron/poll-payment/route.ts:41` (MEDIUM) — `authorizeCron`
  (авторизация всех 8 cron-роутов) без тестов, включая fail-closed при
  незаданном `CRON_SECRET`.
- `apps/web/app/api/admin/telegram-webhook/route.ts:40` (MEDIUM) — `X-Internal-Token`
  без единого негативного теста.
- `.github/workflows/tests.yml:39` + `packages/agent/src/index.ts:301` (MEDIUM ×2)
  — `packages/agent` молча пропускается CI: нет test-скрипта, ноль тестов на
  tool-loop, диспатч денежных tools и Zod-границу, при этом `CLAUDE.md` заявляет
  покрытие («ai: бюджет/роутер»). Завести `vitest.config.ts` + test-скрипт,
  включить в workflow, покрыть tool-loop и `classifyMessage`. ⚠️ Новый чек делать
  обязательным в ruleset только после нескольких зелёных прогонов — падающий
  по своей же ошибке required-чек блокирует разом все merge.

**Пачка 13 — остальное покрытие**
- `apps/web/lib/cabinet/actions.ts:78` (MEDIUM) — 387 строк без тестов:
  ownership-гейты и серверный гейт `status='completed'`.
- `apps/web/app/api/orders/confirm/route.ts:47` (MEDIUM) — роуты инварианта 9:
  порядок «rate-limit до резолва сессии» не зафиксирован тестом.
- `apps/web/lib/telegram/link-flow.ts:1` (MEDIUM) — крупные бот-флоу без прямых
  тестов: `catalog-callbacks` (436 строк), `link-flow` с авто-выставлением счёта
  (199), `vpn-flow` (380).
- `packages/db/src/integration.test.ts:165` (LOW) — RLS-инварианты 7/8 не
  проверяются поведенчески: ни один тест не работает под ролью `anon`.

---

## Что НЕ чинить в этом цикле

- **80 находок уровня LOW без верификации** (`AUDIT-2026-08-10.md`, разделы
  «LOW без верификации»). Они не проходили адверсариальную проверку и могут
  содержать ложные. Разбирать отдельно и по одной, не пачкой.
- **E2E-прогон «заказ → счёт → вебхук → карта»** (`.github/workflows/deploy.yml:93`,
  MEDIUM). Класс отсутствует полностью, но это самостоятельная работа на
  несколько дней — в `docs/BACKLOG.md`, не в этот цикл.
- **Всё, что уже лежит в `docs/BACKLOG.md`** — аудит эти пункты не дублировал.

---

## Открытые вопросы владельцу (ответить до соответствующих пачек)

1. **Пачка 9:** гасить ли уже начисленные самореферальные строки в ledger'е,
   и какая ставка главная — зафиксированная или текущая по статусу?
2. **Пачка 9:** оставить кнопку вывода в кабинете партнёра (с честным текстом о
   сроках) или закрыть флагом до реального исполнителя выплат?
3. **Пачка 1:** финальный опрос провайдера перед захоронением добавляет внешний
   вызов в cron `expire-payments` — приемлемо ли это по времени прогона при
   росте числа заказов, или нужен отдельный джоб?
